Results are written here by `node benchmark/run.mjs`, one file per GitWand
version. They are committed on purpose: the point of a pinned corpus is being
able to compare two versions on identical input.
