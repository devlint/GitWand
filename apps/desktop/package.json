{
  "name": "@gitwand/desktop",
  "version": "0.0.1",
  "private": true,
  "description": "GitWand Desktop — standalone merge conflict resolution (Tauri + Vue 3)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@gitwand/core": "workspace:*",
    "@tauri-apps/api": "^2.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-vue": "^5.1.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vue-tsc": "^2.1.0"
  },
  "license": "MIT"
}
