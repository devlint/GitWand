# v3.6.0 — PR Review 2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use `superpowers:subagent-driven-development`
> (recommended) or `superpowers:executing-plans` to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking. Work the tasks **in order** — later
> tasks depend on types/composables introduced earlier. Every task ends with a named
> conventional commit; the branch must stay green (`pnpm -r test` + `cd apps/desktop &&
> pnpm test`) after each commit.

**Branch:** `feat/pr-review-2` (already created, up to date on `main`).

**Spec (read in full first):** `docs/superpowers/specs/2026-07-09-v3.6.0-pr-review-2-target-ux.md`.
This plan implements workstreams A–F of that spec. Section numbers below (A1, B2, …) map 1:1
to the spec. Do not re-decide anything the spec already fixed.

---

## Goal

Rebuild the in-app PR review around three axes:
1. **Performance** — lazy per-file diff parse, line virtualization, 6→3 network calls on
   the hot path.
2. **Review flow** — GitHub-standard keymap, viewed-file state, pending-review surfacing,
   dismiss/request-reviewers.
3. **AI** — a local-first, opt-in, confidence-scored pre-review pass + PR summary, unified
   with the existing hunk critique and static flags into one `useReviewIntelligence`
   composable behind one settings surface.
Plus: complete the GitLab review methods (largest stub gap).

## Architecture at a glance

- The AI pipeline lives **entirely in `apps/desktop`** (composables). `packages/core` gains
  only one pure, browser-safe helper (`extractImportSources`) consumed by the dependency hop.
- No new Tauri command is needed for the AI pipeline: it reuses `getGitBlame`
  (`backend.ts:1118`), `gitExec`, `getPRDiff`, and `useAIProvider.rawPrompt`
  (`useAIProvider.ts:537`). One new command **is** needed for GitLab F1 (`gl_mr_diff_refs`).
- SWR persistence (`usePrCache.ts`) is extended incrementally for viewed-state (B2), draft
  review (B3), and AI findings/summary keyed by `repo#number@headSha` (C3/D1).
- Keyboard handling is a **pure resolver** (`resolvePrReviewShortcut`) unit-tested in
  isolation (pattern: `resolveTerminalShortcut`, `useTerminalSessions.ts:12-20`), wired by a
  thin listener scoped to the Diff tab in `PrDetailView.vue`. It must not collide with the
  two existing global handlers in `App.vue` (`onGlobalKeydown` @ `App.vue:2476` handles
  `⌘⇧L` + `Escape`; `onKeyDown` @ `App.vue:2622`).

## Global constraints (spec §4 — enforce in every task)

- **IPC**: every new `#[tauri::command]` gets a typed wrapper in `apps/desktop/src/utils/backend*.ts`
  **and** a `dev-server.mjs` route in the same commit. Never `invoke()` outside `backend*.ts`.
  (Task 0 and F1 are the only tasks adding/altering commands.)
- **Parity**: deterministic new commands get parity coverage; the dev-server route must return
  the same shape as Rust. Run `pnpm test:parity` after Task 0 and F1.
- **i18n**: every user-visible string gets a key in **all 5** locales (`en.ts`, `fr.ts`,
  `es.ts`, `pt-br.ts`, `zh-cn.ts` — exact filenames). Add keys in the same commit as the UI
  that uses them; Task G is a final sweep/verification, not a substitute.
- **Settings sync**: any new field goes in **both** `useSettings.ts` (`AppSettings` +
  `DEFAULT_SETTINGS`) and `SettingsPanel.vue` (local `Settings` + default) in the same commit.
- **Sanitize**: any LLM/user-derived HTML goes through `useSafeHtml.ts` (never the hand-rolled
  `renderBody` at `usePrPanel.ts:955`).
- **No unconditional interval**: the AI pipeline is opt-in and visibility-gated (mirror the
  poll discipline at `usePrPanel.ts:990-1020`). Zero work when disabled.
- **core browser-safe**: `packages/core` stays zero-Node. The only core change is a pure
  string helper.
- **Versions**: never hand-edit versions. No `bump-version.sh` in this plan (that's tag-time).

## Test conventions

- Composable/unit tests live in `apps/desktop/src/composables/__tests__/` and
  `apps/desktop/src/utils/__tests__/`. Composable tests **mock `@/utils/backend`** — this is
  the established convention (see `useRepoFileTree.test.ts`, `useGitWand-base-recovery.test.ts`);
  the AGENTS.md "real git repos, no mocked git layer" rule targets Rust tests, not jsdom
  composable tests that mock the IPC boundary.
- Pure functions (resolvers, parsers, mergers) are tested with hand-built inputs, no mocks.
- Parity harness: `apps/desktop/tests/parity/` + `examples/parity_probe.rs`.

---

## Task 0: Surface PR head SHA on `PullRequestDetail` (foundational)

**Why first:** viewed-state invalidation on re-push (B2) and the AI findings/summary cache key
`repo#number@headSha` (C3, D1) both require a head SHA. Neither `PullRequest` nor
`PullRequestDetail` currently carries one (verified: `backend-pr.ts:69` and `:452`).

**Files:**
- Modify: `apps/desktop/src/utils/backend-pr.ts:452` — add `headSha: string;` to
  `PullRequestDetail`; add `headSha: r.head_sha ?? ""` in the snake→camel mapping of
  `ghPrDetail` (and the dev-server branch of the same function).
- Modify Rust detail builders to emit `head_sha`:
  - `apps/desktop/src-tauri/src/commands/gh.rs` — `gh pr view` `--json` list: add `headRefOid`;
    map to `head_sha` in the `PullRequestDetail` struct (struct lives in `commands/*` — add the
    field with `#[serde(rename = "head_sha")]` semantics consistent with siblings).
  - `apps/desktop/src-tauri/src/commands/gitlab.rs:134` `gl_mr_to_detail` — read
    `diff_refs.head_sha` (fallback `sha`).
  - `apps/desktop/src-tauri/src/commands/bitbucket.rs:359` `bb_pr_to_detail` — `source.commit.hash`.
  - `apps/desktop/src-tauri/src/commands/azure.rs:550` `json_to_detail` —
    `lastMergeSourceCommit.commitId` (fallback empty).
  - Add `head_sha: String` to the shared `PullRequestDetail` Rust struct (grep the struct def;
    it is serialized to the frontend).
- Modify: `apps/desktop/dev-server.mjs:3137` (gh-pr-detail route) — add `head_sha: pr.head?.sha ?? ""`.
- Modify parity probe if `gh-pr-detail`/detail is covered (`examples/parity_probe.rs` +
  `tests/parity/`); if detail is not in the parity set, no probe change (note it in commit body).

**Interfaces:**
- Produces: `PullRequestDetail.headSha: string` (empty string when a forge can't provide one —
  callers must treat `""` as "no head SHA known" and skip head-keyed invalidation gracefully).

- [x] **Step 1:** Write `apps/desktop/src/utils/__tests__/backend-pr-headsha.test.ts` — a unit
  test over the `ghPrDetail` dev-server mapping asserting `head_sha` → `headSha`, and that a
  missing field maps to `""`. (Mock `devFetch`/`fetch` per existing backend test pattern.)
- [x] **Step 2:** Add the field to the TS type + mappings and the dev-server route. Run the new
  test — green.
- [x] **Step 3:** Add `head_sha` to the Rust struct + all four detail builders. `cargo build`.
- [x] **Step 4:** `pnpm test:parity` (from `apps/desktop`) — no-op: `PullRequestDetail`/PR-detail
  routes are not in the parity set (`tests/parity/` only covers git-status/log/branches/stash/
  submodule); nothing to run.

**Done when:** `p.prDetail.value.headSha` is populated on GitHub + GitLab against a real repo,
`""` fallback on Bitbucket/Azure when unavailable, all tests green.

**Commit:** `feat(pr-review): surface PR head SHA on PullRequestDetail across forges`

---

## PHASE A — Performance rebuild

### Task A1: Lazy per-file diff parse

**Spec A1.** Keep `getPRDiff` as a single fetch, but stop parsing the whole PR string up front.
Split the raw unified diff by `diff --git` headers into lightweight per-file slices (index
only), and parse a file's hunks **only when it is selected**, caching the parsed result.

**Files:**
- Modify: `apps/desktop/src/composables/usePrPanel.ts`
  - `parseUnifiedDiff` (`:319-365`) stays but is refactored: extract the per-file hunk parse
    into `parseFileDiff(rawFileSlice: string): GitDiff` (the current per-file body).
  - Add `indexDiffFiles(rawDiff: string): { path: string; raw: string }[]` — splits on
    `diff --git a/… b/…` boundaries, extracts the `b/` path, keeps the raw slice. (Reuse the
    existing header regex `usePrPanel.ts:329`.)
  - `loadDiff` (`:631-643`): replace `prDiffFiles.value = parseUnifiedDiff(raw)` with: store
    the index (a new `diffIndex` ref: `{ path; raw }[]`), set `prDiffFiles.value` to
    **shells** (`{ path, hunks: [] }`) so the file sidebar renders instantly, select the first
    file, and parse it lazily.
  - Add `ensureFileParsed(path: string)`: if the shell for `path` has empty hunks and an index
    slice exists, run `parseFileDiff` and replace that entry in `prDiffFiles.value` (cache by
    path — never re-parse). Call it from the `selectedDiffFile` watch and from `selectPr`/first
    file selection.
  - Add a `watch(selectedDiffFile, (p) => p && ensureFileParsed(p))`.
- Keep `selectedDiff` computed (`:295`) working against the now-populated shell.

**Interfaces:**
- Preserve the diff-parsing gotcha (`AGENTS.md`): context detection via `line.startsWith(' ')`
  — do not regress the existing `:358` logic when extracting `parseFileDiff`.

- [x] **Step 1:** Write `apps/desktop/src/composables/__tests__/usePrPanel-lazy-diff.test.ts`:
  - `indexDiffFiles` over a 3-file raw diff → 3 slices with correct `b/` paths and raw bodies.
  - `parseFileDiff` on one slice equals what `parseUnifiedDiff` produced for that file before
    (regression parity — build the expectation from a fixture).
  - Empty-string context lines are classified as context, not added/deleted (gotcha guard).
- [x] **Step 2:** Implement `indexDiffFiles` + `parseFileDiff`; make tests green. (Moved to
  module scope + exported, so they're directly unit-testable; `parseUnifiedDiff` now composes
  them and is kept as a regression-parity helper.)
- [x] **Step 3:** Wire `loadDiff` + `ensureFileParsed` + the `selectedDiffFile` watch. Added a
  test asserting that after `loadDiff`, only the first file has non-empty `hunks` and the rest
  are shells; selecting a second file populates its hunks exactly once (verified the cache guard
  is load-bearing by temporarily removing it and observing the test fail).
- [x] **Step 4:** Verify `hotspots`/`fileHistory` loaders (`:900-914`) still get all file paths
  (they read `prDiffFiles.value.map(f => f.path)` — shells carry `path`, so fine — no code
  change needed).

**Done when:** opening a 50-file PR parses only the selected file; the file sidebar
(`PrDetailView.vue:781`) lists all files; switching files parses on demand, once each.

**Commit:** `perf(pr-review): parse PR diff lazily per selected file`

### Task A2: Diff line virtualization

**Spec A2.** Move `PrInlineDiff.vue` line rendering onto `@tanstack/vue-virtual` (already a dep)
via a shared `useVirtualRows` composable lifted from the variable-height pattern in
`CommitLog.vue:82-94` (`useVirtualizer` + `measureElement`). Bound the DOM regardless of file
size; full render fallback under a threshold (< 300 lines) to keep native find/select.

**Files:**
- Create: `apps/desktop/src/composables/useVirtualRows.ts` — a thin wrapper around
  `useVirtualizer` generalizing the `CommitLog.vue` setup: takes `count` (Ref), a
  `scrollElement` getter, `estimateSize`, `overscan`; returns `{ virtualizer, virtualItems,
  totalSize, measure }`. Include the TDZ-safe post-declaration `setOptions` sync note from
  `CommitLog.vue:96-101,466-472`.
- Modify: `apps/desktop/src/components/PrInlineDiff.vue`
  - The current template nests `v-for hunks` (`:336`) then `v-for lines` (`:390`). Flatten to a
    single row model: a computed `rows: DiffRow[]` where `DiffRow` is a discriminated union
    `{ kind: "hunk-header"; hunkIdx } | { kind: "line"; hunkIdx; lineIdx; dl } | { kind:
    "critique"; hunkIdx } | { kind: "thread"; … } | { kind: "compose"; … }`. (Threads,
    critique panel, and compose box are currently injected between rows — they must become row
    kinds so virtualization measures them.)
  - Under the threshold (`rows.length < 300`): render the existing full DOM (keep current markup
    path) so Cmd+F and text selection work. At/above threshold: render via `useVirtualRows`
    with an absolutely-positioned inner container (pattern `CommitLog.vue:676-699`,
    `:data-index` + `measureElement` ref callback).
  - Expose an imperative `scrollToHunk(hunkIdx)` and `scrollToLine(rowIdx)` (used by B1
    keyboard nav) — resolve the row index for a hunk header / line and call
    `virtualizer.scrollToIndex`. In non-virtual mode, fall back to `el.scrollIntoView`
    (pattern `DiffViewer.vue:186-190`).

**Interfaces:**
- Produces: `PrInlineDiff` exposes (`defineExpose`) `{ scrollToHunk, scrollToLine, rows,
  currentRowIdx }` for the keymap host.
- Keep all existing emits/props unchanged.

- [x] **Step 1:** Write `apps/desktop/src/composables/__tests__/useVirtualRows.test.ts` —
  verify it constructs a virtualizer with the given count/estimate and recomputes on count
  change (mock `@tanstack/vue-virtual`'s `useVirtualizer` and assert options; jsdom has no
  layout so this is a wiring test, not a pixel test).
- [x] **Step 2:** Implement `useVirtualRows`; green.
- [x] **Step 3:** Refactor `PrInlineDiff` to the flat `rows` model. There was no pre-existing
  `PrInlineDiff` test (component tests weren't in the codebase for this file), so a new
  `apps/desktop/src/components/__tests__/PrInlineDiff.test.ts` was added (mounted via native
  `createApp`, matching `LaunchpadView.test`/`MergeEditor.test`) asserting the full-DOM path
  renders every row (hunk-header/line/CI-annotation) and boxes the hunk visually via
  `.pid-rowbox--start/--end` (the per-hunk `.pid-hunk` wrapper div couldn't survive flattening
  to a virtualizable list, so the boxed look is now per-row border/radius classes instead of a
  group wrapper — documented in a code comment).
- [x] **Step 4:** Added the virtual path above the 300-row threshold + `defineExpose({
  scrollToHunk, scrollToLine, rows, currentRowIdx })`. Test asserts a single 5 000-line hunk
  switches to `.pid-rows--virtual` and keeps the live DOM row count under 200 (jsdom has no
  real layout, so the exact visible-window count isn't asserted, only that it stays far below
  the full row count per the plan's own "wiring test, not a pixel test" framing).
- [x] **Step 5:** `pnpm test` green (428 tests, including the 4 new `PrInlineDiff` tests); no
  regression in any existing suite; `vue-tsc --noEmit` clean.

**Done when:** a 5 000-line file's diff DOM is bounded (visible + overscan), scroll is smooth,
< 300-line files still full-render with working native find.

**Commit:** `perf(pr-review): virtualize PR diff line rendering`

### Task A3: Slim detail revalidation (6 → 3 calls)

**Spec A3.** Hot path = `getPR + listComments + listReviews` (3). Defer `getCIChecks` to the
Checks/Diff tab, `gitFileCount` to the Intelligence lazy loaders, `listIssueComments` to the
Info tab.

**Files:**
- Modify: `apps/desktop/src/composables/usePrPanel.ts`
  - `fetchDetailBundle` (`:564-600`): drop `getCIChecks`, `gitFileCount`, `listIssueComments`
    from the `Promise.all` — keep `getPR`, `listComments`, `listReviews`. Update the
    `cache.setDetail` call (bundle no longer includes fresh checks/issueComments/fileCount at
    this point; keep whatever is cached, see below).
  - Add lazy loaders mirroring `loadAnnotations` (`:652`): `loadChecks()` (guarded by a
    `checksLoaded` flag), `loadIssueComments()` (`issueCommentsLoaded`), and move
    `gitFileCount` into `loadHotspots`/`loadFileHistory` (or a `loadRepoFileCount()` called
    from the intelligence tab watch).
  - Update the `watch(detailTab)` (`:687-698`): `diff`/`checks` → `loadChecks()` (+ existing
    `loadAnnotations`); `info` → `loadIssueComments()`; `intelligence` → ensure file count is
    loaded.
  - `usePrCache.DetailBundle` (`usePrCache.ts:37-43`) currently requires `checks` +
    `issueComments`. Make them optional (`checks?`, `issueComments?`) OR keep them and have the
    lazy loaders write-through via `persistDetailCache` (`:612`). Prefer optional fields +
    default `[]` on read to avoid a cache-shape migration; bump nothing — old entries with the
    fields still parse.
  - The 5-min poll (`revalidateOpenDetail` → `fetchDetailBundle`, `:603-607`) now also only
    hits 3 calls. Good — that was half the spec's win.

- [x] **Step 1:** Extend `usePrPanel-*.test.ts` (new file
  `usePrPanel-revalidation.test.ts`): mock the forge, call `selectPr`, assert exactly
  `getPR` + `listComments` + `listReviews` fire and `getCIChecks`/`gitFileCount`/
  `listIssueComments` do **not** on the hot path.
- [x] **Step 2:** Assert switching to the `checks` tab triggers `getCIChecks` once (and not
  again on re-switch); `info` triggers `listIssueComments` once; `intelligence` triggers the
  file count.
- [x] **Step 3:** Implement; made `DetailBundle.checks`/`.issueComments` optional; green.
  (Diff tab also calls `loadChecks()` per the plan's own file note — CI gutter icons on the
  Diff tab need checks too, not just the Checks tab.)
- [x] **Step 4:** Confirmed `totalRepoFiles`-dependent UI (Intelligence panel scope) still
  works once its tab is opened — `loadRepoFileCount()` populates `totalRepoFiles` lazily,
  same ref, no consumer change needed.

**Done when:** opening a PR detail issues 3 forge calls; deferred data loads on its tab, once.

**Commit:** `perf(pr-review): slim detail revalidation to 3 hot-path calls`

---

## PHASE B — Review flow (keyboard-first + viewed)

### Task B1: GitHub-standard keymap (pure resolver + Diff-tab wiring)

**Spec B1.** `J`/`K` next/prev hunk (crosses file boundaries), `⇧J`/`⇧K` next/prev file,
`V` toggle viewed, `⇧V` hide/show viewed, `T` file filter, `C` comment current hunk, `?` help,
`N`/`P` next/prev finding (finding nav lands with C4; reserve them here), `⌘Enter` submit
review (B3). Strict focus guard: inert in any input/textarea/contenteditable/palette.

**Files:**
- Create: `apps/desktop/src/composables/usePrReviewKeymap.ts`
  - Pure resolver `export function resolvePrReviewShortcut(e: KeyboardEvent, ctx: { focused:
    boolean }): PrReviewAction | null` returning a discriminated union: `"next-hunk" |
    "prev-hunk" | "next-file" | "prev-file" | "toggle-viewed" | "toggle-hide-viewed" |
    "filter-files" | "comment-hunk" | "help" | "next-finding" | "prev-finding" |
    "submit-review"`. Mirror `resolveTerminalShortcut` (`useTerminalSessions.ts:12-20`): return
    `null` when `!focused`, when a modifier that doesn't belong is held, or when the key is
    unmapped. `⌘Enter` → `submit-review`; plain letters → their actions; `Shift` distinguishes
    `J/⇧J`, `K/⇧K`, `V/⇧V`.
  - Export `isEditableTarget(el: EventTarget | null): boolean` (input/textarea/select/
    contenteditable) so the host can compute `focused = !isEditableTarget(document.activeElement)
    && diffTabActive`.
- Modify: `apps/desktop/src/components/PrDetailView.vue`
  - Add a `keydown` listener (mounted/unmounted) that: returns early unless
    `p.detailTab.value === 'diff'` and the detail view is open; computes `focused` via
    `isEditableTarget`; calls `resolvePrReviewShortcut`; dispatches. `preventDefault` only on a
    matched action. Must not swallow `Escape`/`⌘⇧L` (leave those to `App.vue`) — the resolver
    doesn't map them, so returning `null` lets them bubble.
  - Dispatch handlers call into the `PrInlineDiff` exposed API (`scrollToHunk`,
    `scrollToLine`) and `usePrPanel` state (file switching via `selectedDiffFile`, viewed
    toggle from B2, comment-hunk opens the compose box on the current hunk, help sets
    `showHelp`, submit-review from B3).
  - Track "current hunk/file" cursor state (a `currentHunkIdx` ref, seeded to 0; `J`/`K` step
    within the flattened row model, crossing into the next/prev file when past the last/first
    hunk — advance `selectedDiffFile` accordingly).
- Modify: `apps/desktop/src/components/HelpView.vue:43-55` — add the PR-review shortcuts to the
  `shortcuts` array with new `help.keyboardShortcuts.*` locale keys (`J`, `K`, `⇧J`, `⇧K`, `V`,
  `⇧V`, `T`, `C`, `N`, `P`, `⌘↵`). Group under a "PR Review" heading if the section supports it.

- [x] **Step 1:** Write `apps/desktop/src/composables/__tests__/usePrReviewKeymap.test.ts` —
  exhaustive resolver cases: each key → action; `Shift` variants; `⌘Enter`; `focused: false`
  → always `null`; unmapped keys → `null`; modifier noise (`Ctrl+J`) → `null` unless mapped.
  Plus `isEditableTarget` over input/textarea/contenteditable/div.
- [x] **Step 2:** Implement the resolver + `isEditableTarget`; green.
- [x] **Step 3:** Wired the listener in `PrDetailView.vue` (extended its existing single
  `handleKeydown`, matching the codebase's one-listener-per-component pattern, rather than
  adding a second `window` listener). Took the plan's own "focused composable test on an
  extracted dispatch function" alternative: extracted the hunk/file cursor + dispatch logic
  into `usePrReviewNav.ts` (business logic belongs in composables — AGENTS.md) instead of a
  full mount test of the very large `PrDetailView.vue`, and unit-tested that directly
  (`usePrReviewNav.test.ts`, 6 cases: next/prev hunk, file-boundary crossing, `⇧J` wrap +
  cursor reset, comment-hunk, help, and the B2/B3/C4 actions being safe no-ops for now).
  `Escape` still closes the fullscreen-image overlay unchanged (same `handleKeydown`, existing
  branch untouched).
- [x] **Step 4:** Added HelpView entries (`pr*` shortcut rows) + 5-locale keys. Locale files are
  actually named `pt-BR.ts`/`zh-CN.ts` in this repo (not the plan's `pt-br.ts`/`zh-cn.ts`) — used
  the real filenames.

**Done when:** a full review is drivable from the keyboard on the Diff tab; typing in a comment
box never triggers navigation; Escape/⌘⇧L still work.

**Commit:** `feat(pr-review): keyboard-first review keymap on the Diff tab`

### Task B2: Viewed-file state

**Spec B2.** Client-side per-PR viewed state in the SWR cache, keyed `repo#number`, invalidated
per file when the head SHA changes (re-push → changed files revert to unviewed). Checkbox per
file in the diff sidebar + "X/N viewed" progress + hide-viewed toggle. Extend
`SubmitReviewOptions` with optional `viewedFiles?: string[]` (optional forge transport; client
is source of truth).

**Files:**
- Modify: `apps/desktop/src/composables/usePrCache.ts`
  - Add a `viewed` record to `PrCacheFile`: `viewed: Record<string, { headSha: string; paths:
    string[]; ts: number }>` keyed by `detailKey(cwd, number)`.
  - Add `getViewed(key): { headSha; paths } | null`, `setViewed(key, headSha, paths)`,
    `toggleViewed(key, headSha, path)` (adds/removes a path; **resets `paths` to `[path]`-less
    when `headSha` differs** from the stored one, then applies the toggle — this is the
    "re-push invalidates viewed" behavior at the whole-PR granularity; if per-file blob-SHA
    granularity is wanted later it's a follow-up, note it). Prune with the existing
    `pruneByAge`/`evictLru` machinery (add to `loadFromStorage`/`saveToStorage`).
- Modify: `apps/desktop/src/composables/forge/types.ts:77-81` — add
  `viewedFiles?: string[];` to `SubmitReviewOptions` (documented as client-tracked, optional
  forge transport; GitHub has no native API so providers may ignore it).
- Modify: `apps/desktop/src/composables/usePrPanel.ts`
  - Add `viewedPaths = ref<Set<string>>()`, load from cache in `selectPr` using
    `prDetail.headSha` (from Task 0). Add `toggleViewed(path)`, `viewedCount` computed,
    `hideViewed = ref(false)`, and a `visibleDiffFiles` computed that filters `prDiffFiles` when
    `hideViewed`. Reset on PR switch.
  - Include `viewedFiles: [...viewedPaths.value]` in `handleSubmitReview`'s `opts` (`:860`).
- Modify: `apps/desktop/src/components/PrDetailView.vue:778-801` (diff sidebar) — per-file
  checkbox bound to `viewedPaths`, a "X/N viewed" progress line, and a "hide viewed" toggle;
  the file list iterates `visibleDiffFiles`. Wire `V`/`⇧V` (B1) to `toggleViewed(current)` /
  `hideViewed` toggle.

- [x] **Step 1:** Write `usePrCache-viewed.test.ts`: `setViewed`/`getViewed` round-trip;
  `toggleViewed` adds then removes; a `toggleViewed` with a **new** `headSha` clears prior
  paths first (re-push invalidation); age/LRU pruning includes the `viewed` record.
- [x] **Step 2:** Implement cache methods; green.
- [x] **Step 3:** Write `usePrPanel-viewed.test.ts`: selecting a PR seeds `viewedPaths` from
  cache; `toggleViewed` updates count + persists; `handleSubmitReview` forwards `viewedFiles`;
  changing `prDetail.headSha` resets viewed on next toggle. (Exported `revalidateOpenDetail`
  from `usePrPanel` — it existed internally for the 5-min poll but wasn't reachable from tests
  or any manual-refresh UI; needed here to simulate a re-push landing.) Also wired
  `toggle-viewed`/`toggle-hide-viewed` into `usePrReviewNav`'s dispatch (was a no-op stub left
  by B1) with 2 new test cases there.
- [x] **Step 4:** Wired the sidebar UI (checkbox per file, progress line, hide-viewed toggle
  button) + 5-locale keys (`pr.viewed.progress`, `.hide`, `.toggle`).

**Done when:** viewed state persists across close/reopen, survives repo switch, resets for
changed files on re-push (headSha change), and the progress bar + hide toggle work.

**Commit:** `feat(pr-review): client-side viewed-file state with re-push invalidation`

### Task B3: Pending review surfaced (badge + resume + ⌘Enter)

**Spec B3.** Persistent counter badge (drafts already survive tab switch; the badge is missing),
resume the draft after close/reopen (persist to SWR cache), and `⌘Enter` submits the review.

**Files:**
- Modify: `apps/desktop/src/composables/usePrCache.ts` — add `drafts: Record<string,
  { comments: PendingReviewComment[]; ts: number }>` keyed `detailKey`; `getDraft`,
  `setDraft`, `clearDraft`. Prune/evict like the others. (Import `PendingReviewComment` from
  `backend.ts`.)
- Modify: `apps/desktop/src/composables/usePrPanel.ts`
  - Persist `draftReviewComments` to the cache on every `handleAddToReview` (`:847`) and clear
    on successful `handleSubmitReview` (`:870`); seed from cache in `selectPr`.
  - Add a `draftCount` computed (already available via `.length`, but expose a named badge
    value). No new interval.
- Modify: `apps/desktop/src/components/PrDetailView.vue` — a persistent badge on the Diff tab
  (or a floating "N in review" chip near the review action) bound to `draftReviewComments.length`,
  visible across tabs while > 0. Wire `⌘Enter` (from B1 `submit-review`) to open the review
  modal / submit (respect `submittingReview`).

- [x] **Step 1:** `usePrCache-draft.test.ts`: draft round-trip + clear + prune.
- [x] **Step 2:** `usePrPanel-draft.test.ts`: `handleAddToReview` persists; `selectPr` restores
  the draft for the same PR; `handleSubmitReview` clears it; switching PRs isolates drafts by
  key.
- [x] **Step 3:** Implement cache + composable wiring; green.
- [x] **Step 4:** `⌘Enter` dispatch wired (opens the review modal via `usePrReviewNav`'s
  `submit-review` case, respecting `submittingReview`). The persistent badge UI **already
  existed** on the always-rendered tab bar (`PrDetailView.vue`'s `.pdv-review-btn`/
  `.pdv-review-count`, bound to `draftReviewComments.length`) — the plan's audit note that "the
  badge is missing" didn't match the current code; the real gap was persistence across detail
  close/reopen, which Steps 1-3 fix. No new locale keys needed (no new UI strings — the badge
  reuses existing markup/keys).

**Done when:** staged comments survive detail close/reopen, the badge reflects the count from
any tab, `⌘Enter` submits.

**Commit:** `feat(pr-review): persist and surface the pending review draft`

### Task B4: Dismiss review + request reviewers

**Spec B4.** New forge methods `dismissReview` and `requestReviewers` (GitHub real; GitLab real
if the API allows, else honest `ForgeNotImplementedError` — never silent degradation). UI in
the Info tab: reviews list + reviewer picker reusing `listReviewerCandidates` (already real on
all 4 forges).

**Files:**
- Modify: `apps/desktop/src/composables/forge/types.ts` — add to `ForgeProvider`:
  `dismissReview(cwd, prNumber, reviewId, message?): Promise<void>` and
  `requestReviewers(cwd, prNumber, logins: string[]): Promise<void>` (both optional `?` on the
  interface so incomplete providers can omit; callers treat absence as unsupported and hide the
  action).
- Implement on providers:
  - `GitHubProvider` — real. Add backend wrappers in `backend-pr.ts` (`ghDismissReview`,
    `ghRequestReviewers`) + Rust commands `gh_dismiss_review` (`gh api … /reviews/:id/dismissals`)
    and `gh_request_reviewers` (`gh api … /requested_reviewers`), registered in `lib.rs`, with
    `dev-server.mjs` routes (parity). Follow the checklist in `src-tauri/CLAUDE.md` §"Ajouter
    une nouvelle commande Tauri".
  - `GitLabProvider` (`GitLabProvider.ts`) — `requestReviewers` real via
    `PUT /merge_requests/:iid?reviewer_ids=…` (add `gl_request_reviewers`); `dismissReview` →
    GitLab has no direct equivalent → `throw new ForgeNotImplementedError("gitlab",
    "dismissReview")`.
  - `BitbucketProvider`/`AzureProvider` — omit the methods (or throw
    `ForgeNotImplementedError`); UI hides the actions.
- Modify: `apps/desktop/src/composables/usePrPanel.ts` — `handleDismissReview(reviewId)` and
  `handleRequestReviewers(logins)` calling the forge, refreshing `prReviews`/`prDetail`,
  surfacing `ForgeNotImplementedError` as a disabled/hidden action (not an error banner).
- Modify: `apps/desktop/src/components/PrDetailView.vue` (Info tab, `:549`) — a reviews list
  with a per-review "Dismiss" button (guarded by capability), and a reviewer picker
  (reuse/mirror the create-form reviewer picker; source from `listReviewerCandidates`).

- [x] **Step 1:** Parity/unit: `backend-pr` wrappers tested against the dev-server mock shape
  (`gh-dismiss-review`, `gh-request-reviewers` routes return `{ ok: true }` on success).
- [x] **Step 2:** `usePrPanel-review-admin.test.ts`: `handleRequestReviewers` calls the forge
  and refreshes; `handleDismissReview` on a provider that throws `ForgeNotImplementedError`
  does **not** set `error` (action treated as unsupported). Found and fixed a real bug while
  writing this test: the check was `err.message?.includes("ForgeNotImplementedError")`, but
  the class only sets that string on `.name`, never in `.message` — so the guard never actually
  matched and would have leaked the error to the banner for real GitLab dismiss attempts. Fixed
  in the two new handlers via `err instanceof ForgeNotImplementedError` (imported from
  `forge/types`). Left the pre-existing `convertDraftToReady` occurrence of the same bug
  untouched (`usePrPanel.ts` — that call path is currently unreachable dead code since no
  provider throws `ForgeNotImplementedError` for `convertDraftToReady`, but fixing unrelated
  pre-existing code is out of this task's scope).
- [x] **Step 3:** Implemented Rust commands (`gh_dismiss_review`, `gh_request_reviewers`) +
  wrappers + dev-server routes; `cargo build` green. `pnpm test:parity` — same no-op as Task 0
  (these commands aren't in the parity set) plus the harness itself couldn't bind a local port
  in this sandboxed environment (`EPERM`, pre-existing/unrelated to this change).
- [x] **Step 4:** GitLab `requestReviewers` real (new `gl_request_reviewers` Rust command,
  resolves usernames → numeric member ids, then `PUT` with `reviewer_ids[]`) + `dismissReview`
  honest `ForgeNotImplementedError` throw. `GitLabProvider.test.ts` created (new file, F1 will
  extend it).
- [x] **Step 5:** Info-tab UI: a "Dismiss" button per non-dismissed review in the timeline, and
  a reviewer-request input (native `<datalist>` sourced from `listReviewerCandidates`, not the
  full custom autocomplete from `PrCreateView.vue` — a lighter-weight but data-source-equivalent
  picker, given the full component is tightly coupled to create-PR-specific state). Both gated
  by `forgeSupportsDismissReview`/`forgeSupportsRequestReviewers` computeds (hidden, not
  erroring, on GitLab/Bitbucket/Azure where unsupported). 5-locale keys added under a new
  `pr.reviews.*` namespace (distinct from the existing singular `pr.review.*` modal namespace).

**Done when:** on GitHub you can dismiss a review and request reviewers from the Info tab;
GitLab can request reviewers and cleanly reports dismiss as unsupported; Bitbucket/Azure hide
the actions.

**Commit:** `feat(pr-review): dismiss review and request reviewers (GitHub real, honest stubs)`

---

## PHASE E1 — Shared line-annotation model (prerequisite for C4)

### Task E1: `LineAnnotation` type + merged annotation stream in `PrInlineDiff`

**Spec E1.** A shared `LineAnnotation` with `source: "ci" | "ai" | "static"`, open severity
(preserve AI confidence/severity rather than the 3-level `mapAnnotation` normalization), and
old/new-side matching (LEFT/RIGHT already modeled by comments). `PrInlineDiff` consumes a merged
CI + AI + static-flags stream with distinct icons/labels per source.

**Files:**
- Create: `apps/desktop/src/composables/prAnnotations.ts` (pure module) — define:
  ```ts
  export interface LineAnnotation {
    source: "ci" | "ai" | "static";
    path: string;
    line: number;                 // anchor line
    side: "LEFT" | "RIGHT";       // default RIGHT (new side)
    endLine?: number;
    severity: string;             // open scale: "failure"|"warning"|"notice"|"risk"|"suggestion"|"nit"|…
    confidence?: number;          // 0-100, AI only
    title: string;
    message?: string;
    checkName?: string;           // ci source
    id?: string;                  // stable id for dismissal/nav (ai)
  }
  export function fromCIAnnotation(a: CIAnnotation): LineAnnotation;   // maps existing v2.18 shape, source "ci"
  export function annotationsByLine(anns: LineAnnotation[], cap?: number): Map<string, LineAnnotation[]>;
  //   key = `${side}:${line}` so LEFT/RIGHT don't collide; reuse the ANN_RANGE_CAP=20 expansion
  export function worstSeverity(anns: LineAnnotation[]): string;
  ```
- Modify: `apps/desktop/src/components/PrInlineDiff.vue`
  - Replace the CI-only `annotationsByLine`/`annotationsForLine` (`:172-195`, new-side only)
    with the shared helpers, matching both sides. Add a new prop
    `annotations?: LineAnnotation[]` (replacing the current `annotations?: CIAnnotation[]` prop
    at `:35`) — the host converts CI annotations via `fromCIAnnotation` and merges AI/static.
    Per-source icon + label (CI: existing `❌/⚠/ℹ`; AI: `AiSparkle` + confidence chip; static:
    a distinct badge).
  - Keep the gutter tooltip; show `confidence` for AI rows.
- Modify: `apps/desktop/src/composables/usePrPanel.ts` — add an `annotationsForFileMerged`
  computed (or defer the merge to E2's `useReviewIntelligence`; for E1 just convert CI via
  `fromCIAnnotation` so the prop type flips without behavior change). AI/static feed lands in
  C4/E2.
- Modify: `apps/desktop/src/components/PrDetailView.vue:814` — pass the converted
  `LineAnnotation[]` to `PrInlineDiff`.

- [x] **Step 1:** `apps/desktop/src/composables/__tests__/prAnnotations.test.ts`:
  `fromCIAnnotation` mapping; `annotationsByLine` keys by side+line and caps ranges at 20;
  LEFT and RIGHT annotations on the same number don't merge; `worstSeverity` ordering.
- [x] **Step 2:** Implement the module; green.
- [x] **Step 3:** Flipped `PrInlineDiff`'s prop to `LineAnnotation[]`, wired the shared helpers.
  CI matching stays RIGHT-only (byte-identical) since `fromCIAnnotation` always sets `side:
  "RIGHT"`; LEFT-side matching on delete lines is purely additive (delete lines never matched
  anything before, since they have no `newLineNo`) — verified via the existing `PrInlineDiff`
  CI-annotation regression test (A2's test file), updated to the new prop shape, still green.
  Also added a confidence chip + source fallback in the tooltip (ready for C4's AI findings)
  without changing CI-only visual output (confidence is `undefined` for CI annotations, so the
  chip doesn't render).
- [x] **Step 4:** Converted at the `usePrPanel` boundary: `lineAnnotationsByFile` (new,
  `LineAnnotation[]`) sits alongside the existing `annotationsByFile` (unchanged, still
  `CIAnnotation[]`, still used for the file-sidebar badge count) rather than replacing it —
  `PrDetailView.vue` now passes `lineAnnotationsByFile` to `PrInlineDiff`. `pnpm test` green
  (491 tests).

**Done when:** CI annotations render exactly as before but through the unified `LineAnnotation`
path, matched on both sides, ready to accept AI + static sources.

**Commit:** `refactor(pr-review): unify diff annotations under a LineAnnotation model`

---

## PHASE E3 — Review AI settings surface (before C, so C can read the opt-ins)

### Task E3: One "Review AI" settings section

**Spec E3.** A single "Review AI" section in Settings: provider inherited from `useAIProvider`,
opt-in pre-review, confidence threshold, cap, opt-in summary. Added to `AppSettings`
(`useSettings.ts`) **and** `SettingsPanel.vue` (sync rule).

**Files:**
- Modify: `apps/desktop/src/composables/useSettings.ts` — add to `AppSettings` (near the AI
  block `:141-160`) and to `DEFAULT_SETTINGS` (`:378`):
  ```ts
  reviewAiPreReview: boolean;     // default false (opt-in)
  reviewAiConfidenceThreshold: number; // default 60
  reviewAiMaxFindings: number;    // default 15
  reviewAiSummary: boolean;       // default false (opt-in)
  ```
- Modify: `apps/desktop/src/components/SettingsPanel.vue` — mirror the four fields in the local
  `Settings` interface (`:147`) + defaults (`:232`), and render controls in the AI section
  (`:558+`): a "Review AI" subsection with toggles (pre-review, summary), a threshold slider
  (0–100), and a cap number input. Reuse `updateSetting`. Gate the subsection behind
  `aiEnabled` (like other AI controls).
- 5-locale keys: `settings.reviewAi.title`, `.preReview`, `.threshold`, `.cap`, `.summary`,
  and helper descriptions.

- [x] **Step 1:** `useSettings-reviewAi.test.ts` (new file — no pre-existing `useSettings` test
  to extend): the four fields exist in `defaultAppSettings` (the plan's `DEFAULT_SETTINGS` name
  doesn't match this codebase's actual export) with the specified defaults, round-trip through
  `saveSettings`/`loadSettings`, and a legacy stored payload missing the new keys backfills
  the defaults on load.
- [x] **Step 2:** Implemented in `useSettings.ts` (`AppSettings` + `defaultAppSettings`) and
  `SettingsPanel.vue` (local `Settings` + its defaults object) in the same commit — sync rule.
  UI: a "Review AI" group in the AI settings tab (pre-review toggle, confidence slider + cap
  number input gated behind it, summary toggle), reusing the existing `sp-group`/`sp-range`/
  `sp-checkbox` patterns from the adjacent LLM-fallback section.
- [x] **Step 3:** Added 5-locale keys under `settings.reviewAi.*`; `vue-tsc --noEmit` clean
  (would catch a missing `LocaleKey` at the `t()` call sites since keys are typed).

**Done when:** the Review AI section renders, toggles persist, defaults match the spec, and the
`AppSettings`/`SettingsPanel` interfaces are in sync.

**Commit:** `feat(pr-review): Review AI settings section (opt-in, threshold, cap, summary)`

---

## PHASE C — AI pre-review pass (local-first, Greptile-style)

### Task C0: Export import-source extraction from core

**Spec C1 hop 2.** The dependency hop extracts import specifiers from touched files
(extraction only, no graph — the resolved graph is v4.0). `parseImport` in
`packages/core/src/resolvers/imports.ts:84` is private; expose a pure extraction helper.

**Files:**
- Modify: `packages/core/src/resolvers/imports.ts` — add and export:
  ```ts
  export function extractImportSources(content: string): string[];
  // scans lines with the existing isImportLike / RE_IMPORT_FROM / RE_REQUIRE / RE_EXPORT_FROM
  // regexes; returns the deduped list of module specifiers (the `source` field). Pure string
  // work — zero Node. Reuses parseImport internally.
  ```
- Modify: `packages/core/src/index.ts:68` — add `extractImportSources` to the public export
  alongside `tryResolveImportConflict, isImportBlock`.

- [x] **Step 1:** `packages/core/src/__tests__/resolvers/import-extract.test.ts` — extracts
  `react`, `../utils`, side-effect imports, `require(...)`, `export … from`; dedupes; ignores
  non-import lines; returns `[]` on empty. (9 cases; core rule ≥ 5.) Found and worked around a
  pre-existing limitation of the reused `RE_EXPORT_FROM` regex: braced named re-exports
  (`export { foo } from …`) aren't matched (the character class is missing `}`), only `export *
  from …` is — this is existing conflict-resolution regex behavior, not something introduced or
  fixed here, so the re-export test case uses the form the regex actually supports and notes why.
- [x] **Step 2:** Implemented; `cd packages/core && pnpm test` green (974 tests). No Node
  import in `resolvers/imports.ts` (browser-safe rule) — confirmed via `grep '^import'` (zero
  matches) and a clean `pnpm build`.

**Done when:** `@gitwand/core` exports `extractImportSources`, tested, still browser-safe.

**Commit:** `feat(core): export extractImportSources for the PR dependency hop`

### Task C1: Pre-review pipeline (hops → findings)

**Spec C1.** Multi-hop, background, opt-in: (1) diff hunks (reuse A1 parse), (2) dependency
extraction + cross-referencing touched paths, (3) blame/history of changed lines, (4) structured
findings via strict JSON.

**Files:**
- Create: `apps/desktop/src/composables/usePrPreReview.ts` (the engine; E2 later folds it into
  `useReviewIntelligence`):
  ```ts
  export interface ReviewFinding {
    id: string;                 // hash of path+line+title (stable, for dismissal/nav)
    path: string; line: number; side: "LEFT" | "RIGHT";
    severity: "risk" | "suggestion" | "nit";
    confidence: number;         // 0-100
    title: string; detail: string;
  }
  ```
  - `buildSystemPrompt(locale)` → asks for strict JSON array of findings
    `{path,line,side,severity,confidence,title,detail}` (mirror the strict-JSON contract style
    of `usePrHunkCritique.ts:30-62`).
  - `buildUserPrompt(file, hunks, importSources, blameContext)` — per file: the hunks (reuse
    `parseFileDiff` output), the extracted import specifiers (from `extractImportSources` over
    the file's new content — obtained via `getPRDiff` slices; for touched-by-N-diff-files
    signal, cross-reference specifiers against the set of paths in the PR), and a compact blame
    summary of changed lines (`getGitBlame` `backend.ts:1118` + last-commit message via
    `gitExec` `["log","-1","--format=%s","-L",…]` or the `useBlameContext` pattern
    `useBlameContext.ts:123-127`).
  - `parseFindings(raw): ReviewFinding[]` — defensive fence/brace recovery exactly like
    `usePrHunkCritique.ts:78-99`, but for an **array**; clamp `confidence` to 0–100; coerce
    unknown severity to `"suggestion"`; drop entries missing `path`/`line`/`title`; assign `id`.
  - `analyzeFile(file, ctx): Promise<ReviewFinding[]>` — one `ai.rawPrompt` call per file
    (the concurrency-1 unit for C3).

- [x] **Step 1:** `usePrPreReview.test.ts` (mocks `useAIProvider` + `@/utils/backend`, 18
  cases): `parseFindings` recovers a JSON array from a ```json fence, from surrounding prose,
  clamps confidence, coerces bad severity, drops malformed entries, assigns stable ids;
  dependency hop cross-reference; blame-summary matching; `analyzeFile` returns `[]` gracefully
  on junk / AI-unavailable.
- [x] **Step 2:** Implemented the prompt builders + parser + `analyzeFile`; green. Adapted from
  plan: `computeImportedByCount`/`summarizeBlameForFile` are separate exported pure helpers
  (not inlined in `analyzeFile`) so the dependency and history hops are independently testable,
  matching the codebase's "pure functions tested with hand-built inputs" convention.
- [x] **Step 3:** Blame hop test: `analyzeFile` calls `getGitBlame(cwd, path)` (whole-file, no
  line-range param exists on that backend function) and includes the matching lines' "author:
  summary" in the prompt. Documented in code that this reflects the *current checkout*, not a
  base-relative blame — no Rust command exists for base-relative blame, and adding one would be
  scope creep beyond C1's "reuse `getGitBlame`" instruction.

**Done when:** given a parsed file + PR context, the engine produces validated `ReviewFinding[]`
with confidence, anchored to lines, robust to malformed LLM output.

**Commit:** `feat(pr-review): local multi-hop pre-review finding engine`

### Task C2: Signal/noise — threshold, cap, dismissal memory

**Spec C2.** User confidence threshold (default 60), top-N cap by confidence (default 15), and
local dismissal memory: a dismissed finding persists per repo (class = `path glob + normalized
title`) and future similar occurrences are suppressed below the threshold.

**Files:**
- Create: `apps/desktop/src/composables/usePrFindingFilter.ts` (pure):
  ```ts
  export function normalizeFindingClass(f: { path: string; title: string }): string;
  //   e.g. dirname-glob(path) + "::" + title.toLowerCase().replace(/\d+/g,"#").trim()
  export function filterFindings(all: ReviewFinding[], opts: {
    threshold: number; cap: number; dismissed: Set<string>;   // set of normalized classes
  }): ReviewFinding[];
  //   drop below threshold; drop dismissed classes; sort by confidence desc; take top `cap`.
  ```
- Modify: `apps/desktop/src/composables/usePrCache.ts` — add per-repo dismissal memory:
  `dismissedFindings: Record<string /*cwd*/, { classes: string[]; ts: number }>` +
  `getDismissed(cwd)`, `addDismissed(cwd, cls)`. Pruned/evicted like the rest.

- [x] **Step 1:** `usePrFindingFilter.test.ts` (8 cases): threshold filtering; cap keeps
  highest confidence; dismissed class removed; `normalizeFindingClass` collapses digit variance
  and path-dirname so "same class, different line/number" matches.
- [x] **Step 2:** `usePrCache-dismissed.test.ts` (7 cases): add/get round-trip, idempotent
  add, per-repo scoping, cold-start persistence, age-pruning.
- [x] **Step 3:** Implemented both; green (527 tests total).

**Done when:** findings shown are ≤ cap, all ≥ threshold, and a dismissed class stays hidden on
recompute and across sessions (same repo).

**Commit:** `feat(pr-review): confidence threshold, cap, and dismissal memory for findings`

### Task C3: Execution — bounded, cancellable, visibility-gated, headSha-cached

**Spec C3.** Work queue with concurrency 1 (each call = a subprocess spawn), cancellable (PR
switch / close), visibility-gated like the poll. SWR cache by `repo#number@headSha` — never
recomputed while head is unchanged. Visible status ("Pre-review: 4/12 files…") with findings
streamed in.

**Files:**
- Create: `apps/desktop/src/composables/usePrReviewQueue.ts` (or fold into `usePrPreReview`):
  - `run(files, ctx, { onFinding, signal })`: sequential `for` over files, `await
    analyzeFile`, emit findings as they land, bail if `signal.aborted` or `document.hidden`
    (re-check between files). Concurrency is 1 by construction.
  - Progress state: `{ done, total, running }` refs.
- Modify: `apps/desktop/src/composables/usePrCache.ts` — findings cache keyed by
  `${detailKey}@${headSha}`: `findings: Record<string, { findings: ReviewFinding[]; ts: number
  }>` + `getFindings(key)`, `setFindings(key, findings)`. On a headSha change the old key simply
  misses → recompute; prune/evict as usual.
- Modify: `apps/desktop/src/composables/usePrPanel.ts` (thin orchestration for now; E2 unifies):
  - Trigger the pipeline on PR select **only if** `settings.reviewAiPreReview` and
    `ai.isAvailable` and `prDetail.headSha` is non-empty. First check the findings cache for
    `key = detailKey@headSha`; on hit, paint instantly and skip compute.
  - Use an `AbortController` reset on PR switch / detail close / `cwd` change (extend the
    existing reset in `resetDetail` `:518` and the `cwd` watcher `:708`).
  - Gate on visibility: pause when `document.hidden`, resume on `visibilitychange` (reuse the
    existing handler wiring `:1011-1025` rather than adding a second listener — extend
    `handleVisibilityChange` to also resume the queue).
  - Expose `preReviewFindings`, `preReviewProgress`, `preReviewRunning`, `dismissFinding(id)`
    (adds the finding's class to `usePrCache.addDismissed` and removes it live).

- [x] **Step 1:** `usePrReviewQueue.test.ts` (4 cases): processes files sequentially (assert
  `analyzeFile` never overlaps via a call-order probe); aborts mid-run when the signal fires;
  stops when `document.hidden` is simulated and resumes after `resume()`; an abort while paused
  unblocks the wait (doesn't hang).
- [x] **Step 2:** `usePrCache-findings.test.ts` (3 cases): findings round-trip by `key@headSha`;
  a different headSha misses; persists across cold start.
- [x] **Step 3:** `usePrPanel-prereview.test.ts` (4 cases): disabled setting → zero `rawPrompt`
  calls; enabled + cache hit → zero compute, instant paint; enabled + miss → runs, populates,
  and writes through to the cache; PR switch aborts the in-flight run and the stale finding
  never reaches the new PR's state nor gets cached under the old PR's key.
- [x] **Step 4:** Implemented. No unconditional interval added — the queue reuses
  `usePrPanel`'s existing single `visibilitychange` listener (extended to call
  `reviewQueue.resume()`) rather than adding a second one, per the perf invariant.

**Done when:** pre-review runs opt-in only, one file at a time, cancels on PR switch, never
recomputes at constant headSha, pauses when hidden, and streams findings with a visible count.

**Commit:** `feat(pr-review): bounded, cancellable, headSha-cached pre-review execution`

### Task C4: Render findings (LineAnnotation + list + N/P nav)

**Spec C4.** Findings anchored in the diff via the generalized annotation overlay (E1), a
confidence-sorted list in the Intelligence panel, and finding-next/prev keyboard nav (`N`/`P`).

**Files:**
- Modify: `apps/desktop/src/composables/prAnnotations.ts` — add `fromFinding(f: ReviewFinding):
  LineAnnotation` (`source: "ai"`, carries `confidence`, `severity`, `id`).
- Modify: `apps/desktop/src/composables/usePrPanel.ts` — merge `preReviewFindings` (converted
  via `fromFinding`, filtered by C2) with CI annotations into the `LineAnnotation[]` stream
  passed per file to `PrInlineDiff` (the `annotationsForFileMerged` computed from E1).
- Modify: `apps/desktop/src/components/PrIntelligencePanel.vue` — a "Pre-review findings"
  section: confidence-sorted list, per-finding severity + confidence chip, a "Dismiss" action
  (calls `dismissFinding`), click-to-jump (sets `selectedDiffFile` + `scrollToLine`), and the
  progress/running indicator. Render `detail` via `useSafeHtml` if it may contain markup.
- Modify: `apps/desktop/src/components/PrDetailView.vue` + B1 keymap — implement `next-finding`
  /`prev-finding` (`N`/`P`): cycle the confidence-sorted findings, switch file + scroll to the
  finding's line, highlight the active finding.

- [x] **Step 1:** `prAnnotations.test.ts` extension: `fromFinding` maps side/line/confidence/id;
  merged stream keeps CI + AI distinct by `source`.
- [x] **Step 2:** `usePrPanel-findings-render.test.ts`: merged annotations for a file include
  both CI and AI entries; dismissing a finding removes it from the stream and the list. (Found
  while writing this test: findings only ever render when `reviewAiPreReview` is on — even a
  cache hit is gated by the opt-in setting, not just live compute. Confirmed that's the intended
  behavior, not a bug, and adjusted the test rather than the code.)
- [x] **Step 3:** Implemented the Intelligence panel's findings list (jump-to-line, dismiss,
  confidence chip, running/progress indicator) + `N`/`P` nav in `usePrReviewNav` (extended, not
  a new file — `next-finding`/`prev-finding` were already-wired no-op cases from B1/C3) +
  5-locale keys under a new `pr.preReview.*` namespace.
- [x] **Step 4:** `pnpm test` green (545 tests); `vue-tsc --noEmit` clean (fixed two knock-on
  type errors: the new `PrIntelligencePanel` props are also required by the legacy, unused-but-
  still-typechecked `PullRequestPanel.vue` — passed harmless defaults there rather than touching
  otherwise-dead code; and exported `DiffRowLike` from `usePrReviewNav.ts` for the test's mock
  typing).

**Done when:** findings show as line-anchored AI annotations + a sorted dismissable list, and
`N`/`P` cycle through them across files.

**Commit:** `feat(pr-review): render and navigate AI pre-review findings`

---

## PHASE D — PR summary

### Task D1: PR summary block on the Info tab

**Spec D1.** A "Summary" block at the top of the Info tab — what / why / affected areas —
generated by one LLM pass over the indexed diff + commit titles, cached by `headSha`,
regenerable on demand, copyable, rendered via `useSafeHtml`.

**Files:**
- Create: `apps/desktop/src/composables/usePrSummary.ts`:
  - `buildSummaryPrompt(diffIndex, commitTitles, locale)` → asks for a short structured summary
    (what / why / affected areas). One `ai.rawPrompt` call.
  - `generate(ctx): Promise<string>` (markdown or plain; rendered safe).
  - Commit titles via `gitExec(["log","--format=%s", base..head])` or the forge — prefer local
    `gitExec` for forge-agnosticism (pattern `useBlameContext.ts`).
- Modify: `apps/desktop/src/composables/usePrCache.ts` — `summaries: Record<string
  /*detailKey@headSha*/, { text: string; ts: number }>` + `getSummary`/`setSummary`.
- Modify: `apps/desktop/src/composables/usePrPanel.ts` — `prSummary` ref, `loadSummary()`
  (opt-in `settings.reviewAiSummary`, cache-first by `detailKey@headSha`, guarded like C3),
  `regenerateSummary()` (bypasses cache), triggered when the Info tab opens if enabled.
- Modify: `apps/desktop/src/components/PrDetailView.vue` (Info tab `:549`) — the Summary block:
  loading/empty/error states, a Regenerate button, a Copy button, `v-html` via `useSafeHtml`
  (never `renderBody`).

- [x] **Step 1:** `usePrSummary.test.ts` (7 cases): `generate` returns the model text; commit
  titles are fetched (with an `origin/<base>..origin/<head>` fallback when the plain range
  fails) and included in the prompt; empty diff / AI-unavailable → graceful `""`.
- [x] **Step 2:** `usePrCache-summary.test.ts`: round-trip by `detailKey@headSha`, a different
  headSha misses, cold-start persistence.
- [x] **Step 3:** `usePrPanel-summary.test.ts` (4 cases): disabled → no call; cache hit → no
  call, instant paint; miss → generates + writes through; `regenerateSummary` bypasses the
  cache and rewrites it.
- [x] **Step 4:** Info-tab UI (loading/empty-via-not-rendering-at-all/regenerate/copy, `v-html`
  via `renderMarkdown` — never the hand-rolled `renderBody`) + 5-locale keys under `pr.summary.*`.

**Done when:** an opt-in summary paints at the top of Info, cached by headSha, regenerable,
copyable, and sanitized.

**Commit:** `feat(pr-review): AI PR summary on the Info tab`

---

## PHASE E2 — Unify the Intelligence pipeline

### Task E2: `useReviewIntelligence` orchestrator

**Spec E2.** One composable orchestrating pre-review (C), summary (D), the migrated hunk
critique (anchored to lines when possible), and the static `aiFlags` — shared cache, single
state. `usePrPanel.ts` **composes** it (doesn't absorb its state).

**Files:**
- Create: `apps/desktop/src/composables/useReviewIntelligence.ts` — composes
  `usePrPreReview`/`usePrReviewQueue` (C), `usePrSummary` (D), `usePrHunkCritique` (existing),
  and the static flags logic currently inline in `PrIntelligencePanel.vue:110-145`
  (extract `computeStaticFlags(diffFiles): LineAnnotation[]` into this composable, `source:
  "static"`, anchored to a line where the flag maps to one). Exposes a single reactive surface:
  `{ findings, summary, hunkCritique, staticFlags, mergedAnnotationsForFile(path),
  progress, running, dismissFinding, regenerateSummary, requestHunkCritique }`.
- Migrate hunk critique anchoring: `usePrHunkCritique` returns a hunk-level verdict; wrap it so
  a critique becomes a `LineAnnotation` anchored to the hunk's first changed line (best-effort)
  in addition to the existing inline panel — keep the existing per-hunk button UX working.
- Refactor: `apps/desktop/src/composables/usePrPanel.ts` — replace the ad-hoc pre-review/summary
  refs added in C3/D1 with a single `const intel = useReviewIntelligence(...)`; re-export what
  the components need. **Net line count of `usePrPanel` should not grow** — the intel state
  moves out. (usePrPanel is 1111 lines; this is the "compose, don't absorb" requirement.)
- Refactor: `apps/desktop/src/components/PrIntelligencePanel.vue` — consume
  `staticFlags`/`findings` from the unified surface instead of computing `aiFlags` locally.
- Refactor: `apps/desktop/src/components/PrInlineDiff.vue` — annotations prop is fed by
  `mergedAnnotationsForFile` (CI + AI + static + critique), one stream.

- [x] **Step 1:** `useReviewIntelligence.test.ts` (7 cases): the merged annotation stream for a
  file (exercised through `usePrPanel`, its real caller) contains CI + AI entries with correct
  `source`; `computeStaticFlags` reproduces the pre-E2 `aiFlags` thresholds (big-file, export
  removal, config/migration file, big hunk, clean diff) as `LineAnnotation`s. **Critique** entries
  are not in the merged stream (see Step 2 note) — the test title's "+critique" is scoped down
  accordingly.
- [x] **Step 2:** Implemented `useReviewIntelligence.ts` (pre-review C1-C4, summary D1, static
  flags ported verbatim-logic from `PrIntelligencePanel.vue`, merged `LineAnnotation` stream).
  **Adapted from plan:** hunk-critique anchoring was *not* wired into the merged stream.
  `usePrHunkCritique`'s state (`critiqueResults`, per-hunk open/loading) lives entirely inside
  `PrInlineDiff.vue`'s own local script — decision #3 explicitly keeps its existing per-hunk
  button UX "in parallel", and there's no live data path from that local, per-component state
  into a shared composable without either (a) hoisting that state up to `usePrPanel`/
  `useReviewIntelligence` (a real UX-affecting rewire, not what decision #3 asked for) or
  (b) adding a dead, untriggered wrapper function. Neither seemed right to force through
  silently, so this is flagged as a gap rather than a plan violation: a future task can hoist
  the critique state if merging it into the gutter overlay is wanted.
- [x] **Step 3:** Refactored `usePrPanel` to compose it (`const intel = useReviewIntelligence(...)`,
  re-exporting its surface under the same public names so `PrDetailView.vue`/
  `PrIntelligencePanel.vue` bindings didn't need to change). Full existing suite green — no
  behavior regressions. `usePrPanel.ts`: 219 lines removed / 24 added net (`git diff --stat`) —
  state moved out, confirming "compose, don't absorb."
- [x] **Step 4:** Refactored `PrIntelligencePanel.vue` to consume `staticFlags` (new prop) from
  the unified surface instead of computing `aiFlags` locally; `PrInlineDiff.vue` needed no
  change — it already consumed `usePrPanel.mergedAnnotationsByFile` (C4), which now sources
  from `useReviewIntelligence` transparently. `pnpm test` green (566 tests).

**Done when:** all AI/static/CI signals flow through one composable and one merged annotation
stream; `usePrPanel` composes rather than owns that state.

**Commit:** `refactor(pr-review): unify AI + static + CI signals in useReviewIntelligence`

---

## PHASE F — Multi-forge: GitLab review completion

### Task F1: GitLab real reviews (inline batch, listReviews, createComment, getFileHistory)

**Spec F1.** Complete the biggest stub gap.

**Files:**
- Add MR diff refs source: Rust command
  `apps/desktop/src-tauri/src/commands/gitlab.rs` `gl_mr_diff_refs(cwd, iid) -> { base_sha,
  start_sha, head_sha }` (reads the MR JSON `diff_refs`; the fetch pattern already exists in
  `gl_mr_to_detail`). Register in `lib.rs`; wrapper `glMrDiffRefs` in `backend-gitlab.ts`;
  `dev-server.mjs` route `/api/gl-mr-diff-refs` (parity). (Alternatively surface `headSha` etc.
  via Task 0's detail if `diff_refs` is added there — but `base_sha`/`start_sha` are needed for
  positioning, so a dedicated refs command is cleanest.)
- Modify: `apps/desktop/src/composables/forge/GitLabProvider.ts`
  - `submitReview` (`:221-249`): after the approve/note handling, if `opts.comments?.length`,
    fetch diff refs once via `glMrDiffRefs`, then for each pending comment call
    `glMrCreateDiscussion` with correct `baseSha/startSha/headSha` and old/new-side anchoring
    (`oldLine` for LEFT, `newLine` for RIGHT). Stop synthesizing a fake `PrReview` when real
    discussions were created — return a representative review reflecting the event.
  - `createComment` (`:177-194`): remove the `as any` casts — read `base_sha/start_sha/head_sha`
    from a typed `CreatePrCommentParams` extension (add optional `base_sha?/start_sha?/head_sha?`
    to the param type in `backend-pr.ts`, or fetch via `glMrDiffRefs` when absent) and pass
    `oldLine`/`newLine` from the comment's side.
  - `listReviews` (`:217`): keep approvals, but also represent COMMENTED / CHANGES_REQUESTED by
    scanning MR notes for the review markers this provider writes (the `**Changes requested:**`
    prefix at `:237`) — return a merged, deduped `PrReview[]` (approvals + note-derived
    verdicts), not approvals-only.
  - `getFileHistory` (`:265-279`): implement the real heuristic — fetch MR notes once
    (`glMrNotes`), count notes mentioning each path, collect reviewers, last comment — instead
    of the zeroed placeholder.

- [x] **Step 1:** `gl_mr_diff_refs` is Tauri-only (no dev-server route — consistent with every
  other GitLab command in this codebase: "GitLab repos are only accessible in the native Tauri
  app context"). Not added to the parity set: it needs a live MR, same documented no-op as
  Task 0/B4's GitHub-side commands.
- [x] **Step 2:** `GitLabProvider.test.ts` extended (9 new cases): `submitReview` with
  `opts.comments` fetches refs once and calls `glMrCreateDiscussion` per comment with correct
  side anchoring; `createComment` passes typed SHAs (no `as any`) and the right old/new line;
  `listReviews` surfaces a note-derived CHANGES_REQUESTED alongside an approval; `getFileHistory`
  counts notes per path (and returns empty placeholders before any PR has loaded — see Step 4
  note on the `prNumber`-less interface).
- [x] **Step 3:** Implemented `gl_mr_diff_refs` (Rust) + `glMrDiffRefs` wrapper; `cargo build`
  green; `pnpm test:parity` — no-op (see Step 1).
- [x] **Step 4:** Implemented the four provider methods. **Adapted from plan:**
  `getFileHistory(cwd, paths)`'s `ForgeProvider` signature carries no `prNumber` (shared across
  all forges; GitHub's version is repo-wide via its own dev-server route), so a real per-MR
  GitLab heuristic needs a `prNumber` from somewhere else — used the most recently opened MR
  (`_lastIid`, set by `getPR`) as the scope, empty history until a PR has loaded. Also added a
  second note-body marker (`**Review:**`) for COMMENT-event review submissions going forward
  (the existing `**Changes requested:**` marker only covered REQUEST_CHANGES) so `listReviews`
  can deterministically derive both verdicts — not retroactive for already-posted notes.

**Done when:** on GitLab, a batch review posts real inline discussions with correct anchors,
`listReviews` reflects comment/changes-requested verdicts, `createComment` is type-clean, and
`getFileHistory` returns real counts.

**Commit:** `feat(pr-review): complete GitLab review methods (inline batch, reviews, history)`

### Task F2: Azure updateComment/deleteComment — honest error + UI hide

**Spec F2.** Azure `updateComment`/`deleteComment` currently throw at runtime. Either implement
if trivial, or keep as `ForgeNotImplementedError` with UI that hides the action (never a crash).
Bitbucket/Azure inline comments stay out of scope.

**Files:**
- Modify: `apps/desktop/src/composables/forge/AzureProvider.ts` — make `updateComment`/
  `deleteComment` throw `ForgeNotImplementedError("azure", …)` (typed, not a bare runtime
  throw) unless the Azure REST edit/delete is trivial to wire (a `az`/REST call analogous to
  existing Azure comment creation) — prefer implementing if the endpoint mirrors an existing
  one; otherwise the typed error.
- Modify: `apps/desktop/src/components/PrCommentThread.vue` (or wherever edit/delete buttons
  live) — hide the edit/delete affordance when the active forge doesn't support it. Add a
  capability signal: `usePrPanel` exposes `forgeSupportsCommentEdit`/`Delete` computed
  (`forge.value.name !== "azure"` for now, or a capability probe), consumed by the thread.
- Ensure `handleEditComment`/`handleDeleteComment` (`usePrPanel.ts:824-840`) swallow
  `ForgeNotImplementedError` into a no-op/hidden action rather than the error banner (same
  pattern as `convertDraftToReady` `:783-784`).

- [x] **Step 1:** `AzureProvider.test.ts`: `updateComment`/`deleteComment` throw
  `ForgeNotImplementedError` (name asserted), not a generic `Error` (unless implemented — then
  assert the backend call).
- [x] **Step 2:** `usePrPanel` capability computed test: edit/delete hidden for Azure, shown for
  GitHub/GitLab.
- [x] **Step 3:** Implement + hide UI affordance; green.

**Adapted from plan:** `AzureProvider.updateComment`/`deleteComment` already threw a typed
`ForgeNotImplementedError` (done in an earlier release, before this session) — Step 1 added test
coverage only, no production change there. The rest of F2 was net-new: `usePrPanel` gained
`forgeSupportsCommentEdit`/`forgeSupportsCommentDelete` computeds (`forge.value.name !== "azure"`,
as specified), `handleEditComment`/`handleDeleteComment` now swallow `ForgeNotImplementedError`
the same way `handleDismissReview`/`handleRequestReviewers` already did, and the capability flows
down as `canEditComments`/`canDeleteComments` props: `PrDetailView.vue` → `PrInlineDiff.vue` →
`PrCommentThread.vue` (`canEdit`/`canDelete`, default `true`), which hides the ✏️/🗑 buttons
individually rather than the whole `.pct-actions` block, so a forge that supports one but not the
other degrades correctly too.

**Done when:** Azure never crashes on edit/delete — the actions are hidden or cleanly report
unsupported.

**Commit:** `fix(pr-review): honest Azure comment edit/delete capability, no runtime crash`

---

## PHASE G — Finalization

### Task G: i18n verification + CHANGELOG

**Files:**
- All 5 locale files (`apps/desktop/src/locales/{en,fr,es,pt-br,zh-cn}.ts`) — verify every key
  added across tasks B–F exists in all five, with real translations (not English placeholders).
- `CHANGELOG.md` (repo root) — add a single `## [Unreleased]` entry (or append to the existing
  one) grouped by `### Added / Changed / Fixed`, covering: lazy diff parse + virtualization +
  3-call revalidation; keyboard keymap; viewed-file state; pending-review surfacing; dismiss/
  request-reviewers; AI pre-review (opt-in, confidence, cap, dismissal memory); PR summary;
  unified Intelligence pipeline; GitLab review completion; Azure comment capability fix.
  **No version bump. No `website/changelog.md` edit** (both are tag-time, per AGENTS.md).

- [ ] **Step 1:** Run a locale-key audit (grep each new `t('…')`/`LocaleKey` against all five
  files, or a small script) — zero missing keys; `dev:web` shows no missing-key warnings.
- [ ] **Step 2:** Write the `[Unreleased]` CHANGELOG entry.
- [ ] **Step 3:** Full green gate: `pnpm -r test` + `cd apps/desktop && pnpm test` +
  `pnpm test:parity` + `cd packages/core && pnpm test`.

**Done when:** all 5 locales complete, CHANGELOG updated (Unreleased only), every suite green.

**Commit:** `docs(changelog): PR Review 2.0 Unreleased entry + i18n sync`

---

## Success metrics checklist (spec §3 — verify before merge)

- [ ] Detail open = 3 forge calls on the hot path (was 6). (Task A3 test.)
- [ ] 50-file PR parses only the selected file. (Task A1 test.)
- [ ] 5 000-line file diff DOM is bounded. (Task A2 manual perf note in PR.)
- [ ] Full review drivable via `J/K/⇧J/⇧K/V/⇧V/T/C/N/P/⌘Enter`, resolver unit-tested. (B1/C4.)
- [ ] AI findings ≤ cap, all ≥ threshold, line-anchored, dismissable with memory. (C2/C4.)
- [ ] Pipeline opt-in, zero calls when off, aborts on PR switch, never recomputes at constant
  headSha. (C3 test.)
- [ ] `pnpm -r test` green, parity green for new commands, all 5 locales complete. (Task G.)

## Open decisions for the human checkpoint

1. **Viewed-state invalidation granularity (B2):** the plan invalidates viewed state at the
   whole-PR level on any headSha change (simplest, matches "re-push resets viewed" honestly).
   Per-file blob-SHA granularity (only changed files revert) is noted as a follow-up — confirm
   the coarser behavior is acceptable for v3.6.0.
2. **`gl_mr_diff_refs` vs. extending Task 0 detail (F1):** the plan adds a dedicated refs
   command because inline positioning needs `base_sha` + `start_sha`, not just `head_sha`.
   Confirm we don't instead want to fold all three SHAs into `PullRequestDetail` in Task 0.
3. **Hunk-critique anchoring (E2):** migrating the existing hunk-level verdict to a
   line-anchored `LineAnnotation` is best-effort (anchors to the hunk's first changed line).
   Confirm keeping the existing per-hunk button UX in parallel is desired (plan assumes yes).
4. **Azure comment edit/delete (F2):** plan defaults to a typed `ForgeNotImplementedError` +
   hidden UI unless the REST endpoint is trivial. Confirm we don't require a full Azure
   implementation in this release.
