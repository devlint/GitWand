# v3.1.0 — Terminal PTY à onglets + Launch Claude Code — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer le runner de commandes git (`GitTerminal`) par un vrai terminal PTY à onglets, et faire que "Launch Claude Code" ouvre une session agent réellement utilisable dans un onglet PTY intégré.

**Architecture:** Backend Rust : un module `terminal.rs` qui spawn des shells dans des PTY (`portable-pty`), registre global de sessions, streaming d'output via `tauri::ipc::Channel`. Frontend : composable `useTerminalSessions` (état des onglets scopé par repo + debounce refresh) + composant `TerminalPanel.vue` rendant `xterm.js` (lazy). `AgentSessionsPanel` lance `claude` dans un onglet PTY.

**Tech Stack:** Rust + Tauri 2 (`portable-pty`), Vue 3 `<script setup>`, `@xterm/xterm` + `@xterm/addon-fit`, Vitest.

## Global Constraints

- Package manager : **pnpm uniquement** (jamais npm/yarn).
- **Ne jamais éditer les versions à la main** — `package.json`/`Cargo.toml`/`tauri.conf.json` sont gérés par `./scripts/bump-version.sh`. (Ajouter une *dépendance* dans `Cargo.toml`/`package.json` est autorisé ; ne pas toucher le champ `version`.)
- Toute commande `#[tauri::command]` Rust → wrapper typé dans `apps/desktop/src/utils/backend.ts` **dans la même tâche** + route fonctionnelle (non-stub) dans `apps/desktop/dev-server.mjs`.
- Tout chemin user-supplied → `safe_repo_path()` (Rust) / `safeRepoPath()` (dev-server). Jamais d'interpolation shell : args discrets.
- Ne jamais injecter de secrets (`GH_TOKEN`, etc.) dans l'env d'un shell visible.
- Composant Vue : `<script setup>` Composition API. Logique métier dans `composables/`, pas dans les composants.
- Toute string user-visible : clé dans les **5 locales** (`en`, `fr`, `es`, `pt-BR`, `zh-CN`), jamais de texte hardcodé.
- Perf : tout panel `v-if` défaut-faux → `defineAsyncComponent` (lazy). Aucun `setInterval` inconditionnel. Instances xterm **hors réactivité** Vue.
- Boutons : jamais de pilule/`border-radius: 9999px` — utiliser `--radius-sm/md/lg`.
- Versions à utiliser : `@xterm/xterm@^6.0.0`, `@xterm/addon-fit@^0.11.0`, `portable-pty = "0.9"` (vérifier la dernière stable sur crates.io au moment de l'impl).
- Nouvelle dépendance Cargo : mesurer impact compile + taille binaire et le consigner dans la PR.

---

## File Structure

**Backend (Rust)**
- Create `apps/desktop/src-tauri/src/commands/terminal.rs` — module PTY (registre, 4 commandes, helper shell, cleanup).
- Modify `apps/desktop/src-tauri/src/commands/mod.rs` — déclarer `pub(crate) mod terminal;`.
- Modify `apps/desktop/src-tauri/src/lib.rs` — enregistrer les 4 commandes dans `generate_handler!` + cleanup au `WindowEvent::Destroyed`.
- Modify `apps/desktop/src-tauri/Cargo.toml` — ajouter `portable-pty`.

**Frontend (TS/Vue)**
- Modify `apps/desktop/src/utils/backend.ts` — wrappers `terminalOpen/Write/Resize/Close`.
- Modify `apps/desktop/src/utils/backend-core.ts` — helper d'abstraction du transport output (Channel Tauri vs EventSource dev).
- Create `apps/desktop/src/composables/useTerminalSessions.ts` — état onglets scopé repo + debounce + focus flag.
- Create `apps/desktop/src/composables/__tests__/useTerminalSessions.test.ts`.
- Create `apps/desktop/src/components/TerminalPanel.vue` — UI onglets + hôtes xterm.
- Modify `apps/desktop/src/App.vue` — remplacer overlay `GitTerminal` par `TerminalPanel` ; routage raccourcis ; lancement agent → onglet.
- Delete `apps/desktop/src/components/GitTerminal.vue`.
- Modify `apps/desktop/src/components/AgentSessionsPanel.vue` — supprimer faux `active`, re-poll, émettre vers onglet PTY.
- Modify `apps/desktop/src/composables/useSettings.ts` + `apps/desktop/src/components/SettingsPanel.vue` — `terminalFontSize`, `terminalShell`.
- Modify `apps/desktop/src/locales/{en,fr,es,pt-br,zh-cn}.ts` — clés i18n.

**Dev parity**
- Modify `apps/desktop/dev-server.mjs` — routes terminal (echo PTY minimal + SSE).
- Modify `apps/desktop/package.json` — deps xterm.

---

## Task 1 : Backend PTY — module `terminal.rs` + dépendance

**Files:**
- Create: `apps/desktop/src-tauri/src/commands/terminal.rs`
- Modify: `apps/desktop/src-tauri/src/commands/mod.rs`
- Modify: `apps/desktop/src-tauri/src/lib.rs:272` (generate_handler) + window event
- Modify: `apps/desktop/src-tauri/Cargo.toml`

**Interfaces:**
- Produces (commandes Tauri, consommées par Task 4) :
  - `terminal_open(cwd: String, shell: Option<String>, cols: u16, rows: u16, on_output: tauri::ipc::Channel<String>) -> Result<u64, String>`
  - `terminal_write(id: u64, data: String) -> Result<(), String>`
  - `terminal_resize(id: u64, cols: u16, rows: u16) -> Result<(), String>`
  - `terminal_close(id: u64) -> Result<(), String>`
  - `terminal_close_all()` (interne, appelée au quit)

- [ ] **Step 1 : Ajouter la dépendance**

Dans `apps/desktop/src-tauri/Cargo.toml`, section `[dependencies]` :

```toml
portable-pty = "0.9"
```

- [ ] **Step 2 : Vérifier que ça compile (dépendance résolue)**

Run: `cd apps/desktop/src-tauri && cargo build`
Expected: build OK (la dépendance se télécharge et compile).

- [ ] **Step 3 : Écrire le module `terminal.rs`**

Create `apps/desktop/src-tauri/src/commands/terminal.rs` :

```rust
//! Terminal PTY intégré : spawn de shells interactifs dans des PTY,
//! streaming d'output vers le frontend via `tauri::ipc::Channel`.

use std::collections::HashMap;
use std::io::{Read, Write};
use std::sync::atomic::{AtomicU64, Ordering};
use std::sync::{Mutex, OnceLock};

use portable_pty::{native_pty_system, CommandBuilder, PtySize};
use tauri::ipc::Channel;

/// Une session PTY vivante. Le thread lecteur est détaché ; il sort sur EOF.
struct PtyHandle {
    master: Box<dyn portable_pty::MasterPty + Send>,
    writer: Box<dyn Write + Send>,
    child: Box<dyn portable_pty::Child + Send + Sync>,
}

fn sessions() -> &'static Mutex<HashMap<u64, PtyHandle>> {
    static S: OnceLock<Mutex<HashMap<u64, PtyHandle>>> = OnceLock::new();
    S.get_or_init(|| Mutex::new(HashMap::new()))
}

static NEXT_ID: AtomicU64 = AtomicU64::new(1);

/// Résout le shell à lancer : override explicite, sinon $SHELL (Unix) /
/// %ComSpec% ou powershell (Windows).
fn resolve_shell(shell: &Option<String>) -> String {
    if let Some(s) = shell {
        if !s.trim().is_empty() {
            return s.clone();
        }
    }
    #[cfg(windows)]
    {
        std::env::var("ComSpec").unwrap_or_else(|_| "powershell.exe".to_string())
    }
    #[cfg(not(windows))]
    {
        std::env::var("SHELL").unwrap_or_else(|_| "/bin/bash".to_string())
    }
}

#[tauri::command]
pub(crate) fn terminal_open(
    cwd: String,
    shell: Option<String>,
    cols: u16,
    rows: u16,
    on_output: Channel<String>,
) -> Result<u64, String> {
    if cwd.trim().is_empty() {
        return Err("cwd must not be empty".to_string());
    }
    // Validation du cwd : doit être un dossier existant et résolu (anti-traversal).
    let canon = std::fs::canonicalize(&cwd).map_err(|e| format!("invalid cwd: {e}"))?;
    if !canon.is_dir() {
        return Err("cwd is not a directory".to_string());
    }

    let pty_system = native_pty_system();
    let pair = pty_system
        .openpty(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 })
        .map_err(|e| format!("openpty failed: {e}"))?;

    let shell_path = resolve_shell(&shell);
    let mut cmd = CommandBuilder::new(&shell_path);
    // Login shell sur Unix pour charger le profil utilisateur (PATH, aliases…).
    #[cfg(not(windows))]
    cmd.arg("-l");
    cmd.cwd(&canon);
    // On NE propage PAS de tokens GitWand : le shell charge ses propres creds.
    // Enrichissement PATH (Homebrew / npm global) repris de hidden_cmd.
    if let Some(extra) = enriched_path() {
        cmd.env("PATH", extra);
    }

    let child = pair
        .slave
        .spawn_command(cmd)
        .map_err(|e| format!("spawn shell failed: {e}"))?;

    let mut reader = pair
        .master
        .try_clone_reader()
        .map_err(|e| format!("clone reader failed: {e}"))?;
    let writer = pair
        .master
        .take_writer()
        .map_err(|e| format!("take writer failed: {e}"))?;

    let id = NEXT_ID.fetch_add(1, Ordering::SeqCst);

    sessions().lock().unwrap().insert(
        id,
        PtyHandle { master: pair.master, writer, child },
    );

    // Thread lecteur : pousse les chunks vers le frontend.
    std::thread::spawn(move || {
        let mut buf = [0u8; 8192];
        loop {
            match reader.read(&mut buf) {
                Ok(0) => break, // EOF : shell terminé
                Ok(n) => {
                    let chunk = String::from_utf8_lossy(&buf[..n]).to_string();
                    if on_output.send(chunk).is_err() {
                        break; // frontend parti
                    }
                }
                Err(_) => break,
            }
        }
        // Nettoyage : retirer la session du registre quand le PTY se ferme.
        sessions().lock().unwrap().remove(&id);
    });

    Ok(id)
}

#[tauri::command]
pub(crate) fn terminal_write(id: u64, data: String) -> Result<(), String> {
    let mut map = sessions().lock().unwrap();
    let h = map.get_mut(&id).ok_or("session not found")?;
    h.writer
        .write_all(data.as_bytes())
        .map_err(|e| format!("write failed: {e}"))?;
    h.writer.flush().map_err(|e| format!("flush failed: {e}"))
}

#[tauri::command]
pub(crate) fn terminal_resize(id: u64, cols: u16, rows: u16) -> Result<(), String> {
    let map = sessions().lock().unwrap();
    let h = map.get(&id).ok_or("session not found")?;
    h.master
        .resize(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 })
        .map_err(|e| format!("resize failed: {e}"))
}

#[tauri::command]
pub(crate) fn terminal_close(id: u64) -> Result<(), String> {
    if let Some(mut h) = sessions().lock().unwrap().remove(&id) {
        let _ = h.child.kill();
    }
    Ok(())
}

/// Tue toutes les sessions (appelé au quit de l'app).
pub(crate) fn terminal_close_all() {
    let mut map = sessions().lock().unwrap();
    for (_, mut h) in map.drain() {
        let _ = h.child.kill();
    }
}

/// PATH enrichi (Homebrew / chemins usuels) — mirror minimal de hidden_cmd.
/// Retourne None si rien à ajouter.
fn enriched_path() -> Option<String> {
    let current = std::env::var("PATH").unwrap_or_default();
    #[cfg(target_os = "macos")]
    {
        let extras = ["/opt/homebrew/bin", "/opt/homebrew/sbin", "/usr/local/bin"];
        let mut path = current.clone();
        for e in extras {
            if !current.split(':').any(|p| p == e) {
                path = format!("{e}:{path}");
            }
        }
        return Some(path);
    }
    #[allow(unreachable_code)]
    {
        let _ = current;
        None
    }
}
```

- [ ] **Step 4 : Déclarer le module**

Dans `apps/desktop/src-tauri/src/commands/mod.rs`, ajouter (ordre alpha, après `scratch`) :

```rust
pub(crate) mod terminal;
```

- [ ] **Step 5 : Enregistrer les commandes + cleanup**

Dans `apps/desktop/src-tauri/src/lib.rs`, dans `tauri::generate_handler![ ... ]` (vers la ligne 409, près de `agent_session_launch`), ajouter :

```rust
            commands::terminal::terminal_open,
            commands::terminal::terminal_write,
            commands::terminal::terminal_resize,
            commands::terminal::terminal_close,
```

Puis brancher le cleanup au quit. Localiser le `.build(...)`/`.run(...)` ou le `on_window_event`. Si un handler `on_window_event` existe, y ajouter ; sinon en ajouter un sur le builder :

```rust
        .on_window_event(|_window, event| {
            if let tauri::WindowEvent::Destroyed = event {
                commands::terminal::terminal_close_all();
            }
        })
```

- [ ] **Step 6 : Compiler le backend**

Run: `cd apps/desktop/src-tauri && cargo build`
Expected: PASS (pas d'erreur, warnings tolérés).

- [ ] **Step 7 : Commit**

```bash
git add apps/desktop/src-tauri/
git commit -m "feat(terminal): backend PTY module (portable-pty) + 4 commands"
```

---

## Task 2 : Wrappers `backend.ts` + parité dev:web

**Files:**
- Modify: `apps/desktop/src/utils/backend.ts`
- Modify: `apps/desktop/src/utils/backend-core.ts`
- Modify: `apps/desktop/dev-server.mjs`
- Modify: `apps/desktop/package.json` (deps xterm — installées ici pour Task 4)

**Interfaces:**
- Consumes : commandes Rust de Task 1.
- Produces (consommé par Task 3/4) :
  - `terminalOpen(cwd: string, opts: { shell?: string; cols: number; rows: number }, onOutput: (chunk: string) => void): Promise<number>`
  - `terminalWrite(id: number, data: string): Promise<void>`
  - `terminalResize(id: number, cols: number, rows: number): Promise<void>`
  - `terminalClose(id: number): Promise<void>`

- [ ] **Step 1 : Installer les dépendances xterm**

Run: `cd apps/desktop && pnpm add @xterm/xterm@^6.0.0 @xterm/addon-fit@^0.11.0`
Expected: ajoutées à `package.json`, `pnpm-lock.yaml` mis à jour.

- [ ] **Step 2 : Ajouter les wrappers dans `backend.ts`**

Dans `apps/desktop/src/utils/backend.ts`, après la section agent sessions (~ligne 3032). Le streaming diffère selon le mode : Tauri = `Channel`, dev = `EventSource`.

```ts
import { Channel } from "@tauri-apps/api/core";

/**
 * Open a PTY shell session. `onOutput` is called with each raw output chunk.
 * Returns the backend session id (used by write/resize/close).
 */
export async function terminalOpen(
  cwd: string,
  opts: { shell?: string; cols: number; rows: number },
  onOutput: (chunk: string) => void,
): Promise<number> {
  if (isTauri()) {
    const channel = new Channel<string>();
    channel.onmessage = onOutput;
    return tauriInvoke<number>("terminal_open", {
      cwd,
      shell: opts.shell ?? null,
      cols: opts.cols,
      rows: opts.rows,
      onOutput: channel,
    });
  }
  // Dev mode : SSE. Le serveur renvoie l'id en premier message JSON, puis
  // les chunks bruts.
  const id = await devTerminalOpen(cwd, opts, onOutput);
  return id;
}

export async function terminalWrite(id: number, data: string): Promise<void> {
  if (isTauri()) {
    await tauriInvoke("terminal_write", { id, data });
    return;
  }
  await devFetch(`${DEV_SERVER}/api/terminal-write`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ id, data }),
  });
}

export async function terminalResize(id: number, cols: number, rows: number): Promise<void> {
  if (isTauri()) {
    await tauriInvoke("terminal_resize", { id, cols, rows });
    return;
  }
  await devFetch(`${DEV_SERVER}/api/terminal-resize`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ id, cols, rows }),
  });
}

export async function terminalClose(id: number): Promise<void> {
  if (isTauri()) {
    await tauriInvoke("terminal_close", { id });
    return;
  }
  await devFetch(`${DEV_SERVER}/api/terminal-close`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ id }),
  });
}
```

- [ ] **Step 3 : Helper SSE dev dans `backend-core.ts`**

Dans `apps/desktop/src/utils/backend-core.ts`, ajouter :

```ts
/**
 * Dev-mode PTY open over Server-Sent Events. First SSE message is
 * `{"id": <number>}`; subsequent messages are raw output chunks (JSON-encoded
 * strings to preserve control bytes).
 */
export async function devTerminalOpen(
  cwd: string,
  opts: { shell?: string; cols: number; rows: number },
  onOutput: (chunk: string) => void,
): Promise<number> {
  const params = new URLSearchParams({
    cwd,
    cols: String(opts.cols),
    rows: String(opts.rows),
  });
  if (opts.shell) params.set("shell", opts.shell);
  return new Promise((resolve, reject) => {
    const es = new EventSource(`${DEV_SERVER}/api/terminal-open?${params}`);
    let resolved = false;
    es.onmessage = (ev) => {
      const payload = JSON.parse(ev.data);
      if (!resolved && typeof payload?.id === "number") {
        resolved = true;
        resolve(payload.id);
      } else if (typeof payload?.chunk === "string") {
        onOutput(payload.chunk);
      } else if (payload?.eof) {
        es.close();
      }
    };
    es.onerror = () => {
      if (!resolved) reject(new Error("dev terminal SSE failed"));
      es.close();
    };
  });
}
```

(`devTerminalOpen` doit être importé dans `backend.ts` depuis `./backend-core`.)

- [ ] **Step 4 : Routes dev-server**

Dans `apps/desktop/dev-server.mjs`, après les routes existantes (avant le 404 final), ajouter un PTY echo minimal via `child_process`. Maintenir un `Map` `devPtys`.

```js
// ── Terminal PTY (dev echo) ───────────────────────────────────────────────
const devPtys = new Map(); // id -> { proc, res }
let devPtyNextId = 1;

// SSE open
if (url.pathname === "/api/terminal-open" && req.method === "GET") {
  const cwd = url.searchParams.get("cwd") || process.cwd();
  const shell = url.searchParams.get("shell") || process.env.SHELL || "/bin/bash";
  const id = devPtyNextId++;
  res.writeHead(200, {
    "Content-Type": "text/event-stream",
    "Cache-Control": "no-cache",
    Connection: "keep-alive",
  });
  res.write(`data: ${JSON.stringify({ id })}\n\n`);
  // Shell en mode pipe (pas de vrai TTY en dev — best effort).
  const proc = spawn(shell, ["-i"], { cwd, env: process.env });
  const send = (buf) =>
    res.write(`data: ${JSON.stringify({ chunk: buf.toString() })}\n\n`);
  proc.stdout.on("data", send);
  proc.stderr.on("data", send);
  proc.on("close", () => {
    res.write(`data: ${JSON.stringify({ eof: true })}\n\n`);
    devPtys.delete(id);
  });
  devPtys.set(id, { proc, res });
  req.on("close", () => { proc.kill(); devPtys.delete(id); });
  return;
}
if (url.pathname === "/api/terminal-write" && req.method === "POST") {
  const { id, data } = await readJson(req);
  devPtys.get(id)?.proc.stdin.write(data);
  return sendJson(res, { ok: true });
}
if (url.pathname === "/api/terminal-resize" && req.method === "POST") {
  return sendJson(res, { ok: true }); // pas de winsize en mode pipe
}
if (url.pathname === "/api/terminal-close" && req.method === "POST") {
  const { id } = await readJson(req);
  devPtys.get(id)?.proc.kill();
  devPtys.delete(id);
  return sendJson(res, { ok: true });
}
```

(Adapter `readJson`/`sendJson` aux helpers existants du fichier — repérer le pattern d'une route POST voisine comme `/api/write-file`.)

- [ ] **Step 5 : Vérifier le typecheck front**

Run: `cd apps/desktop && pnpm vue-tsc --noEmit`
Expected: PASS (pas d'erreur de type sur les nouveaux wrappers).

- [ ] **Step 6 : Vérifier que le dev-server démarre**

Run: `cd apps/desktop && node -c dev-server.mjs`
Expected: PASS (syntaxe valide).

- [ ] **Step 7 : Commit**

```bash
git add apps/desktop/src/utils/backend.ts apps/desktop/src/utils/backend-core.ts apps/desktop/dev-server.mjs apps/desktop/package.json apps/desktop/../pnpm-lock.yaml
git commit -m "feat(terminal): backend.ts wrappers + dev:web SSE parity + xterm deps"
```

---

## Task 3 : Composable `useTerminalSessions` (TDD)

**Files:**
- Create: `apps/desktop/src/composables/useTerminalSessions.ts`
- Test: `apps/desktop/src/composables/__tests__/useTerminalSessions.test.ts`

**Interfaces:**
- Consumes : `terminalOpen/Write/Resize/Close` (Task 2).
- Produces (consommé par Task 4/5/6) :
  - `interface TerminalTab { id: number; sessionId: number; title: string; titleManual: boolean; alive: boolean }`
  - `useTerminalSessions()` retourne :
    - `tabsFor(repoPath: string): TerminalTab[]`
    - `activeTabId(repoPath: string): number | null`
    - `openTab(repoPath: string, cwd: string, onChunk: (sessionId: number, chunk: string) => void): Promise<TerminalTab>`
    - `closeTab(repoPath: string, tabId: number): Promise<void>`
    - `setActive(repoPath: string, tabId: number): void`
    - `renameTab(repoPath: string, tabId: number, title: string): void`
    - `setTitleFromShell(repoPath: string, tabId: number, title: string): void` (ignoré si `titleManual`)
    - `disposeRepo(repoPath: string): Promise<void>`
    - `notifyOutput(repoPath: string): void` (arme le debounce refresh)
    - `setMutationHandler(cb: (repoPath: string) => void): void`
    - `terminalFocused: Ref<boolean>` (partagé, posé par le panel)

Note : le composable NE touche PAS xterm. Le rendu xterm + le câblage `onChunk → term.write` vivent dans le panel (Task 4).

- [ ] **Step 1 : Écrire les tests (échouent)**

Create `apps/desktop/src/composables/__tests__/useTerminalSessions.test.ts` :

```ts
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";

vi.mock("../../utils/backend", () => ({
  terminalOpen: vi.fn(async () => 42),
  terminalWrite: vi.fn(async () => {}),
  terminalResize: vi.fn(async () => {}),
  terminalClose: vi.fn(async () => {}),
}));

import { useTerminalSessions } from "../useTerminalSessions";
import { terminalClose } from "../../utils/backend";

describe("useTerminalSessions", () => {
  beforeEach(() => {
    // Réinitialiser l'état module entre les tests.
    const s = useTerminalSessions();
    for (const p of ["/repo/a", "/repo/b"]) {
      for (const tab of [...s.tabsFor(p)]) s.tabsFor(p) && (s as any);
    }
  });

  it("scope les onglets par repoPath", async () => {
    const s = useTerminalSessions();
    await s.openTab("/repo/a", "/repo/a", () => {});
    expect(s.tabsFor("/repo/a")).toHaveLength(1);
    expect(s.tabsFor("/repo/b")).toHaveLength(0);
  });

  it("openTab crée un onglet actif avec un sessionId backend", async () => {
    const s = useTerminalSessions();
    const tab = await s.openTab("/repo/a", "/repo/a", () => {});
    expect(tab.sessionId).toBe(42);
    expect(s.activeTabId("/repo/a")).toBe(tab.id);
    expect(tab.alive).toBe(true);
  });

  it("closeTab appelle le backend et retire l'onglet", async () => {
    const s = useTerminalSessions();
    const tab = await s.openTab("/repo/a", "/repo/a", () => {});
    await s.closeTab("/repo/a", tab.id);
    expect(terminalClose).toHaveBeenCalledWith(42);
    expect(s.tabsFor("/repo/a")).toHaveLength(0);
  });

  it("setTitleFromShell n'écrase pas un titre manuel", async () => {
    const s = useTerminalSessions();
    const tab = await s.openTab("/repo/a", "/repo/a", () => {});
    s.renameTab("/repo/a", tab.id, "build");
    s.setTitleFromShell("/repo/a", tab.id, "vim");
    expect(s.tabsFor("/repo/a")[0].title).toBe("build");
  });

  it("notifyOutput appelle le mutation handler après debounce", async () => {
    vi.useFakeTimers();
    const s = useTerminalSessions();
    const cb = vi.fn();
    s.setMutationHandler(cb);
    await s.openTab("/repo/a", "/repo/a", () => {});
    s.notifyOutput("/repo/a");
    expect(cb).not.toHaveBeenCalled();
    vi.advanceTimersByTime(800);
    expect(cb).toHaveBeenCalledWith("/repo/a");
    vi.useRealTimers();
  });

  it("disposeRepo ferme toutes les sessions du repo", async () => {
    const s = useTerminalSessions();
    await s.openTab("/repo/b", "/repo/b", () => {});
    await s.openTab("/repo/b", "/repo/b", () => {});
    await s.disposeRepo("/repo/b");
    expect(s.tabsFor("/repo/b")).toHaveLength(0);
    expect(terminalClose).toHaveBeenCalled();
  });
});
```

(Le `beforeEach` de reset est approximatif — l'ajuster à l'API réelle une fois le composable écrit : exposer un `__resetForTests()` si nécessaire.)

- [ ] **Step 2 : Lancer les tests (échouent)**

Run: `cd apps/desktop && pnpm vitest run src/composables/__tests__/useTerminalSessions.test.ts`
Expected: FAIL (`useTerminalSessions` n'existe pas).

- [ ] **Step 3 : Écrire le composable**

Create `apps/desktop/src/composables/useTerminalSessions.ts` :

```ts
import { ref, reactive, type Ref } from "vue";
import {
  terminalOpen,
  terminalWrite,
  terminalResize,
  terminalClose,
} from "../utils/backend";

export interface TerminalTab {
  id: number;          // id local (mémoire)
  sessionId: number;   // id PTY backend
  title: string;
  titleManual: boolean;
  alive: boolean;
}

// État module (pattern store-composable, pas de Pinia).
const tabsByRepo = reactive(new Map<string, TerminalTab[]>());
const activeByRepo = reactive(new Map<string, number | null>());
let nextLocalId = 1;

// Debounce refresh par repo.
const debounceTimers = new Map<string, ReturnType<typeof setTimeout>>();
let mutationHandler: ((repoPath: string) => void) | null = null;
const DEBOUNCE_MS = 800;

const terminalFocused = ref(false);

function listFor(repoPath: string): TerminalTab[] {
  if (!tabsByRepo.has(repoPath)) tabsByRepo.set(repoPath, []);
  return tabsByRepo.get(repoPath)!;
}

export function useTerminalSessions() {
  function tabsFor(repoPath: string): TerminalTab[] {
    return listFor(repoPath);
  }

  function activeTabId(repoPath: string): number | null {
    return activeByRepo.get(repoPath) ?? null;
  }

  async function openTab(
    repoPath: string,
    cwd: string,
    onChunk: (sessionId: number, chunk: string) => void,
  ): Promise<TerminalTab> {
    const tab: TerminalTab = {
      id: nextLocalId++,
      sessionId: -1,
      title: "shell",
      titleManual: false,
      alive: true,
    };
    const list = listFor(repoPath);
    list.push(tab);
    activeByRepo.set(repoPath, tab.id);
    // Taille initiale standard ; le panel re-fit au mount.
    const sessionId = await terminalOpen(
      cwd,
      { cols: 80, rows: 24 },
      (chunk) => onChunk(tab.sessionId, chunk),
    );
    tab.sessionId = sessionId;
    return tab;
  }

  async function closeTab(repoPath: string, tabId: number): Promise<void> {
    const list = listFor(repoPath);
    const idx = list.findIndex((t) => t.id === tabId);
    if (idx === -1) return;
    const [tab] = list.splice(idx, 1);
    if (tab.sessionId >= 0) await terminalClose(tab.sessionId);
    if (activeByRepo.get(repoPath) === tabId) {
      activeByRepo.set(repoPath, list.length ? list[list.length - 1].id : null);
    }
  }

  function setActive(repoPath: string, tabId: number): void {
    activeByRepo.set(repoPath, tabId);
  }

  function renameTab(repoPath: string, tabId: number, title: string): void {
    const tab = listFor(repoPath).find((t) => t.id === tabId);
    if (tab) {
      tab.title = title;
      tab.titleManual = true;
    }
  }

  function setTitleFromShell(repoPath: string, tabId: number, title: string): void {
    const tab = listFor(repoPath).find((t) => t.id === tabId);
    if (tab && !tab.titleManual && title.trim()) tab.title = title;
  }

  async function disposeRepo(repoPath: string): Promise<void> {
    const list = listFor(repoPath);
    for (const tab of list) {
      if (tab.sessionId >= 0) await terminalClose(tab.sessionId);
    }
    tabsByRepo.set(repoPath, []);
    activeByRepo.set(repoPath, null);
  }

  function notifyOutput(repoPath: string): void {
    const prev = debounceTimers.get(repoPath);
    if (prev) clearTimeout(prev);
    debounceTimers.set(
      repoPath,
      setTimeout(() => {
        debounceTimers.delete(repoPath);
        mutationHandler?.(repoPath);
      }, DEBOUNCE_MS),
    );
  }

  function setMutationHandler(cb: (repoPath: string) => void): void {
    mutationHandler = cb;
  }

  function write(sessionId: number, data: string) {
    return terminalWrite(sessionId, data);
  }
  function resize(sessionId: number, cols: number, rows: number) {
    return terminalResize(sessionId, cols, rows);
  }

  return {
    tabsFor,
    activeTabId,
    openTab,
    closeTab,
    setActive,
    renameTab,
    setTitleFromShell,
    disposeRepo,
    notifyOutput,
    setMutationHandler,
    write,
    resize,
    terminalFocused: terminalFocused as Ref<boolean>,
  };
}
```

- [ ] **Step 4 : Lancer les tests (passent)**

Run: `cd apps/desktop && pnpm vitest run src/composables/__tests__/useTerminalSessions.test.ts`
Expected: PASS. (Ajuster le `beforeEach`/reset des tests à l'API réelle si besoin — viser 6 tests verts.)

- [ ] **Step 5 : Commit**

```bash
git add apps/desktop/src/composables/useTerminalSessions.ts apps/desktop/src/composables/__tests__/useTerminalSessions.test.ts
git commit -m "feat(terminal): useTerminalSessions composable (tabs scoped per repo + debounce)"
```

---

## Task 4 : `TerminalPanel.vue` + remplacement de GitTerminal

**Files:**
- Create: `apps/desktop/src/components/TerminalPanel.vue`
- Modify: `apps/desktop/src/App.vue:58` (import) + `:2706-2710` (overlay) + `:1252` (state)
- Delete: `apps/desktop/src/components/GitTerminal.vue`

**Interfaces:**
- Consumes : `useTerminalSessions` (Task 3), `terminalWrite/Resize` via le composable, `@xterm/xterm`, `@xterm/addon-fit`, `useSettings` (Task 7 — utiliser un défaut `13`/`""` si Task 7 pas encore faite).
- Props : `{ repoPath: string }`. Emits : `(e: "close")`.

- [ ] **Step 1 : Écrire `TerminalPanel.vue`**

Create `apps/desktop/src/components/TerminalPanel.vue`. Points clés : xterm hors réactivité (`Map` simple), `onTitleChange` → `setTitleFromShell`, `FitAddon` + `ResizeObserver` → `resize`, hauteur drag persistée en `localStorage`, focus → `terminalFocused`.

```vue
<script setup lang="ts">
import { ref, onMounted, onBeforeUnmount, watch, nextTick, computed } from "vue";
import { useTerminalSessions, type TerminalTab } from "../composables/useTerminalSessions";
import { useI18n } from "../composables/useI18n";

const props = defineProps<{ repoPath: string }>();
const emit = defineEmits<{ (e: "close"): void }>();
const { t } = useI18n();

const sessions = useTerminalSessions();
const tabs = computed(() => sessions.tabsFor(props.repoPath));
const activeId = computed(() => sessions.activeTabId(props.repoPath));

// xterm hors réactivité.
type XtermEntry = { term: any; fit: any; el: HTMLElement };
const xterms = new Map<number, XtermEntry>(); // key = tab.id
let XtermCtor: any = null;
let FitCtor: any = null;

const hostRefs = ref<Record<number, HTMLElement | undefined>>({});

// Hauteur du panneau (persistée).
const HEIGHT_KEY = "gitwand-terminal-height";
const height = ref(Number(localStorage.getItem(HEIGHT_KEY)) || 260);

async function ensureXtermLibs() {
  if (XtermCtor) return;
  const [{ Terminal }, { FitAddon }] = await Promise.all([
    import("@xterm/xterm"),
    import("@xterm/addon-fit"),
  ]);
  XtermCtor = Terminal;
  FitCtor = FitAddon;
  // CSS xterm (importé une fois).
  await import("@xterm/xterm/css/xterm.css");
}

async function mountTab(tab: TerminalTab) {
  await ensureXtermLibs();
  await nextTick();
  const el = hostRefs.value[tab.id];
  if (!el || xterms.has(tab.id)) return;
  const term = new XtermCtor({ fontSize: 13, cursorBlink: true });
  const fit = new FitCtor();
  term.loadAddon(fit);
  term.open(el);
  fit.fit();
  // Output déjà câblé via openTab(onChunk) au niveau App ; ici on relie
  // l'écriture clavier + titres.
  term.onData((data: string) => sessions.write(tab.sessionId, data));
  term.onTitleChange((title: string) =>
    sessions.setTitleFromShell(props.repoPath, tab.id, title),
  );
  xterms.set(tab.id, { term, fit, el });
  // Resize observer.
  const ro = new ResizeObserver(() => {
    fit.fit();
    sessions.resize(tab.sessionId, term.cols, term.rows);
  });
  ro.observe(el);
}

// Le panel doit recevoir les chunks. App passe un dispatcher : on expose
// writeChunk pour qu'App route les chunks vers le bon term.
function writeChunk(sessionId: number, chunk: string) {
  for (const [, e] of xterms) {
    if (e.term.__sessionId === sessionId) {
      e.term.write(chunk);
      return;
    }
  }
}
defineExpose({ writeChunk });

watch(tabs, async () => {
  for (const tab of tabs.value) {
    await mountTab(tab);
    const e = xterms.get(tab.id);
    if (e) e.term.__sessionId = tab.sessionId;
  }
  // Nettoyer les xterms d'onglets fermés.
  for (const id of [...xterms.keys()]) {
    if (!tabs.value.some((t) => t.id === id)) {
      xterms.get(id)?.term.dispose();
      xterms.delete(id);
    }
  }
}, { immediate: true, deep: false });

function onNew() {
  // App orchestre openTab (il fournit onChunk) ; émettre un event.
  emit("close"); // placeholder remplacé ci-dessous par un vrai event "new"
}

function onFocusIn() { sessions.terminalFocused.value = true; }
function onFocusOut() { sessions.terminalFocused.value = false; }

// Renommage inline.
const editingId = ref<number | null>(null);
const editValue = ref("");
function startRename(tab: TerminalTab) { editingId.value = tab.id; editValue.value = tab.title; }
function commitRename(tab: TerminalTab) {
  if (editValue.value.trim()) sessions.renameTab(props.repoPath, tab.id, editValue.value.trim());
  editingId.value = null;
}

// Drag resize.
let dragStartY = 0; let dragStartH = 0;
function onDragStart(e: MouseEvent) {
  dragStartY = e.clientY; dragStartH = height.value;
  window.addEventListener("mousemove", onDragMove);
  window.addEventListener("mouseup", onDragEnd);
}
function onDragMove(e: MouseEvent) {
  height.value = Math.max(120, Math.min(700, dragStartH + (dragStartY - e.clientY)));
}
function onDragEnd() {
  localStorage.setItem(HEIGHT_KEY, String(height.value));
  window.removeEventListener("mousemove", onDragMove);
  window.removeEventListener("mouseup", onDragEnd);
}

onBeforeUnmount(() => {
  for (const [, e] of xterms) e.term.dispose();
  xterms.clear();
});
</script>

<template>
  <div class="tp" :style="{ height: height + 'px' }" @focusin="onFocusIn" @focusout="onFocusOut">
    <div class="tp__drag" @mousedown="onDragStart" />
    <div class="tp__tabs">
      <button v-for="tab in tabs" :key="tab.id" class="tp__tab"
        :class="{ 'tp__tab--active': tab.id === activeId }"
        @click="sessions.setActive(props.repoPath, tab.id)"
        @dblclick="startRename(tab)">
        <input v-if="editingId === tab.id" v-model="editValue" class="tp__rename"
          @keyup.enter="commitRename(tab)" @blur="commitRename(tab)" />
        <span v-else>{{ tab.title }}</span>
        <span class="tp__close" @click.stop="sessions.closeTab(props.repoPath, tab.id)">×</span>
      </button>
      <button class="tp__new" :title="t('terminal.newTab')" @click="$emit('new')">+</button>
      <button class="tp__hide" :title="t('terminal.hide')" @click="emit('close')">⌄</button>
    </div>
    <div class="tp__body">
      <div v-for="tab in tabs" :key="tab.id" class="tp__host"
        :ref="(el) => (hostRefs[tab.id] = el as HTMLElement)"
        v-show="tab.id === activeId" />
    </div>
  </div>
</template>

<style scoped>
.tp { display: flex; flex-direction: column; border-top: 1px solid var(--border); background: var(--bg-elevated); }
.tp__drag { height: 5px; cursor: ns-resize; }
.tp__tabs { display: flex; gap: 2px; align-items: center; padding: 0 6px; }
.tp__tab { display: flex; gap: 6px; align-items: center; padding: 4px 10px; border-radius: var(--radius-sm); background: transparent; border: none; cursor: pointer; }
.tp__tab--active { background: var(--bg-base); }
.tp__close { opacity: .6; }
.tp__new, .tp__hide { margin-left: auto; border: none; background: transparent; cursor: pointer; padding: 4px 8px; border-radius: var(--radius-sm); }
.tp__new { margin-left: 4px; }
.tp__body { flex: 1; position: relative; overflow: hidden; }
.tp__host { position: absolute; inset: 0; }
.tp__rename { width: 90px; }
</style>
```

Note implémentation : l'`emit("new")` doit être déclaré dans `defineEmits`. Corriger le `defineEmits` :
```ts
const emit = defineEmits<{ (e: "close"): void; (e: "new"): void }>();
```
et supprimer le `onNew` placeholder.

- [ ] **Step 2 : Brancher dans `App.vue`**

Dans `apps/desktop/src/App.vue` :

Remplacer l'import (`:58`) :
```ts
const TerminalPanel = defineAsyncComponent(() => import("./components/TerminalPanel.vue"));
```
(et supprimer la ligne `const GitTerminal = ...`)

Remplacer l'overlay (`:2706-2710`) par un panneau ancré bas (pas un overlay backdrop) :
```vue
<TerminalPanel
  v-if="showTerminal && repoFolderPath"
  ref="terminalPanelRef"
  :repo-path="repoFolderPath"
  @close="showTerminal = false"
  @new="openTerminalTab()"
/>
```

Ajouter le ref + l'orchestration `openTerminalTab` (le `onChunk` route vers le panel) près de `:1252` :
```ts
import { useTerminalSessions } from "./composables/useTerminalSessions";
const termSessions = useTerminalSessions();
const terminalPanelRef = ref<any>(null);

async function openTerminalTab(cwd?: string) {
  if (!repoFolderPath.value) return;
  showTerminal.value = true;
  await termSessions.openTab(
    repoFolderPath.value,
    cwd ?? repoFolderPath.value,
    (sessionId, chunk) => {
      terminalPanelRef.value?.writeChunk(sessionId, chunk);
      termSessions.notifyOutput(repoFolderPath.value!);
    },
  );
}

// Refresh debounced.
termSessions.setMutationHandler((repoPath) => {
  if (repoPath === repoFolderPath.value) repoRefresh();
});
```

- [ ] **Step 3 : Supprimer `GitTerminal.vue`**

Run: `git rm apps/desktop/src/components/GitTerminal.vue`
Expected: fichier supprimé. Vérifier qu'aucune autre référence ne subsiste : `grep -rn "GitTerminal" apps/desktop/src` → 0 résultat.

- [ ] **Step 4 : Typecheck + build front**

Run: `cd apps/desktop && pnpm vue-tsc --noEmit`
Expected: PASS.

- [ ] **Step 5 : Test manuel dev:web**

Run: `cd apps/desktop && pnpm dev:web` (dans un terminal) puis ouvrir le navigateur, ouvrir un repo, ouvrir le terminal.
Expected: un onglet shell s'ouvre, on peut taper une commande (`ls`, `echo hi`) et voir la sortie ; `+` ouvre un 2e onglet ; `×` ferme ; double-clic renomme ; le panneau se redimensionne.

- [ ] **Step 6 : Commit**

```bash
git add apps/desktop/src/components/TerminalPanel.vue apps/desktop/src/App.vue
git rm apps/desktop/src/components/GitTerminal.vue
git commit -m "feat(terminal): TerminalPanel.vue (xterm tabs) replacing GitTerminal"
```

---

## Task 5 : Raccourcis routés par focus (TDD)

**Files:**
- Modify: `apps/desktop/src/App.vue:2032-2046` (onKeyDown)
- Create: `apps/desktop/src/composables/__tests__/terminalShortcuts.test.ts` (teste une fonction extraite)

**Interfaces:**
- Consumes : `useTerminalSessions().terminalFocused` (Task 3).
- Produces : `resolveTerminalShortcut(e, focused): "new" | "close" | { switch: number } | null` — fonction pure extraite pour testabilité, placée dans `useTerminalSessions.ts`.

- [ ] **Step 1 : Écrire le test (échoue)**

Create `apps/desktop/src/composables/__tests__/terminalShortcuts.test.ts` :

```ts
import { describe, it, expect } from "vitest";
import { resolveTerminalShortcut } from "../useTerminalSessions";

function key(k: string, opts: Partial<KeyboardEvent> = {}): KeyboardEvent {
  return { key: k, metaKey: true, ctrlKey: false, shiftKey: false, ...opts } as KeyboardEvent;
}

describe("resolveTerminalShortcut", () => {
  it("ne fait rien si le terminal n'a pas le focus", () => {
    expect(resolveTerminalShortcut(key("t"), false)).toBeNull();
  });
  it("Cmd+T => new quand focus terminal", () => {
    expect(resolveTerminalShortcut(key("t"), true)).toBe("new");
  });
  it("Cmd+W => close quand focus terminal", () => {
    expect(resolveTerminalShortcut(key("w"), true)).toBe("close");
  });
  it("Cmd+3 => switch index 2 quand focus terminal", () => {
    expect(resolveTerminalShortcut(key("3"), true)).toEqual({ switch: 2 });
  });
  it("ignore les touches non gérées", () => {
    expect(resolveTerminalShortcut(key("q"), true)).toBeNull();
  });
});
```

- [ ] **Step 2 : Lancer (échoue)**

Run: `cd apps/desktop && pnpm vitest run src/composables/__tests__/terminalShortcuts.test.ts`
Expected: FAIL (`resolveTerminalShortcut` non exporté).

- [ ] **Step 3 : Implémenter la fonction pure**

Dans `apps/desktop/src/composables/useTerminalSessions.ts`, ajouter (export top-level) :

```ts
export type TerminalShortcut = "new" | "close" | { switch: number } | null;

/** Route un keydown vers une action terminal si le terminal a le focus. */
export function resolveTerminalShortcut(e: KeyboardEvent, focused: boolean): TerminalShortcut {
  if (!focused) return null;
  const mod = e.metaKey || e.ctrlKey;
  if (!mod) return null;
  if (e.key === "t") return "new";
  if (e.key === "w") return "close";
  if (/^[1-9]$/.test(e.key)) return { switch: Number(e.key) - 1 };
  return null;
}
```

- [ ] **Step 4 : Lancer (passe)**

Run: `cd apps/desktop && pnpm vitest run src/composables/__tests__/terminalShortcuts.test.ts`
Expected: PASS.

- [ ] **Step 5 : Câbler dans `App.vue`**

Dans `onKeyDown` (`App.vue:2032`), AVANT le `if (mod && e.key === "t")` existant :

```ts
  const termAction = resolveTerminalShortcut(e, termSessions.terminalFocused.value);
  if (termAction) {
    e.preventDefault();
    if (termAction === "new") openTerminalTab();
    else if (termAction === "close") {
      const active = termSessions.activeTabId(repoFolderPath.value!);
      if (active != null) termSessions.closeTab(repoFolderPath.value!, active);
    } else {
      const tabs = termSessions.tabsFor(repoFolderPath.value!);
      const target = tabs[termAction.switch];
      if (target) termSessions.setActive(repoFolderPath.value!, target.id);
    }
    return;
  }
```

(Importer `resolveTerminalShortcut` depuis le composable.)

- [ ] **Step 6 : Typecheck + tous les tests**

Run: `cd apps/desktop && pnpm vue-tsc --noEmit && pnpm vitest run src/composables/__tests__/`
Expected: PASS.

- [ ] **Step 7 : Commit**

```bash
git add apps/desktop/src/composables/useTerminalSessions.ts apps/desktop/src/composables/__tests__/terminalShortcuts.test.ts apps/desktop/src/App.vue
git commit -m "feat(terminal): focus-routed Cmd+T/W/1-9 shortcuts"
```

---

## Task 6 : Lancement de session agent dans un onglet PTY

**Files:**
- Modify: `apps/desktop/src/components/AgentSessionsPanel.vue:74-86` (launch) + emits
- Modify: `apps/desktop/src/App.vue` (handler de l'event launch → onglet PTY)

**Interfaces:**
- Consumes : `openTerminalTab(cwd)` (Task 4), `termSessions.write` (Task 3), `agentSessionList` (re-poll).
- Produces : event `(e: "launch-agent", payload: { path: string; tool: string })` depuis `AgentSessionsPanel`.

- [ ] **Step 1 : Réécrire `launch()` dans `AgentSessionsPanel.vue`**

Remplacer la fonction `launch` (`:73-86`) — supprimer le faux `active`/`setTimeout`, émettre vers App, re-poll :

```ts
const emit = defineEmits<{
  (e: "close"): void;
  (e: "open-tab", path: string): void;
  (e: "launch-agent", payload: { path: string; tool: string }): void;
}>();

async function launch(session: AgentSession) {
  launching.value = session.path;
  error.value = null;
  try {
    if (session.tool === "cursor" || session.tool === "windsurf") {
      // Éditeurs GUI : ouverture externe via la commande existante.
      await agentSessionLaunch(session.path, session.tool);
    } else {
      // claude / other : onglet PTY intégré.
      emit("launch-agent", { path: session.path, tool: "claude" });
    }
    // État réel : re-poll au lieu d'un faux active optimiste.
    await loadSessions();
  } catch (err: any) {
    error.value = t("agents.errorLaunch").replace("{0}", String(err?.message ?? err));
  } finally {
    launching.value = null;
  }
}
```

- [ ] **Step 2 : Handler dans `App.vue`**

Là où `<AgentSessionsPanel>` est rendu, ajouter le handler :

```vue
<AgentSessionsPanel
  ...
  @launch-agent="onLaunchAgent"
/>
```

```ts
async function onLaunchAgent(payload: { path: string; tool: string }) {
  await openTerminalTab(payload.path);
  const tabs = termSessions.tabsFor(repoFolderPath.value!);
  const tab = tabs[tabs.length - 1];
  if (tab) await termSessions.write(tab.sessionId, `${payload.tool}\n`);
}
```

- [ ] **Step 3 : Vérifier qu'aucun faux `active` ne subsiste**

Run: `grep -n "setTimeout" apps/desktop/src/components/AgentSessionsPanel.vue`
Expected: aucune occurrence liée à `active`.

- [ ] **Step 4 : Typecheck**

Run: `cd apps/desktop && pnpm vue-tsc --noEmit`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add apps/desktop/src/components/AgentSessionsPanel.vue apps/desktop/src/App.vue
git commit -m "feat(agents): launch claude in integrated PTY tab, drop fake active state"
```

---

## Task 7 : Settings `terminalFontSize` + `terminalShell`

**Files:**
- Modify: `apps/desktop/src/composables/useSettings.ts` (interface `AppSettings` + défauts)
- Modify: `apps/desktop/src/components/SettingsPanel.vue` (interface `Settings` + UI)
- Modify: `apps/desktop/src/components/TerminalPanel.vue` (utiliser `terminalFontSize`)

**Interfaces:**
- Produces : `AppSettings.terminalFontSize: number` (défaut 13), `AppSettings.terminalShell: string` (défaut "").

- [ ] **Step 1 : Ajouter à `AppSettings`**

Dans `useSettings.ts`, ajouter à l'interface `AppSettings` et à l'objet de défauts :

```ts
  terminalFontSize: number;  // taille police terminal (défaut 13)
  terminalShell: string;     // override shell ("" = auto)
```
Défauts :
```ts
  terminalFontSize: 13,
  terminalShell: "",
```

- [ ] **Step 2 : Ajouter à `Settings` (SettingsPanel.vue) + UI**

Dans `SettingsPanel.vue`, ajouter les deux champs à l'interface locale `Settings`, et une section UI (réutiliser le pattern d'un champ existant : un `<input type="number">` pour la taille, un `<input type="text">` pour le shell). Toutes les strings via `t(...)`.

- [ ] **Step 3 : Utiliser dans `TerminalPanel.vue`**

Remplacer `fontSize: 13` en dur par la valeur des settings, et passer `terminalShell` à `openTab` (propager jusqu'à `terminalOpen` via une option). Mettre à jour la signature `openTab` pour accepter `{ shell?: string }` si nécessaire, OU lire les settings dans `openTerminalTab` (App) et étendre `terminalOpen` opts. Choix simple : lire `useSettings()` dans `App.openTerminalTab` et passer `shell` à un `openTab` étendu :

```ts
// useTerminalSessions.openTab signature étendue :
async function openTab(repoPath, cwd, onChunk, opts?: { shell?: string; fontSize?: number })
// → terminalOpen(cwd, { cols, rows, shell: opts?.shell }, ...)
```

- [ ] **Step 4 : Vérifier la synchro des deux interfaces**

Run: `grep -n "terminalFontSize\|terminalShell" apps/desktop/src/composables/useSettings.ts apps/desktop/src/components/SettingsPanel.vue`
Expected: présent dans les DEUX fichiers.

- [ ] **Step 5 : Typecheck + tests**

Run: `cd apps/desktop && pnpm vue-tsc --noEmit && pnpm vitest run src/composables/__tests__/useTerminalSessions.test.ts`
Expected: PASS (adapter les tests si `openTab` change de signature — opts optionnel, les tests existants restent valides).

- [ ] **Step 6 : Commit**

```bash
git add apps/desktop/src/composables/useSettings.ts apps/desktop/src/components/SettingsPanel.vue apps/desktop/src/components/TerminalPanel.vue apps/desktop/src/composables/useTerminalSessions.ts
git commit -m "feat(terminal): terminalFontSize + terminalShell settings"
```

---

## Task 8 : i18n 5 locales

**Files:**
- Modify: `apps/desktop/src/locales/en.ts`, `fr.ts`, `es.ts`, `pt-br.ts`, `zh-cn.ts`

**Interfaces:** clés ajoutées : `terminal.newTab`, `terminal.hide`, `terminal.rename`, `terminal.close`, `settings.terminalFontSize`, `settings.terminalShell`, `settings.terminalShellHint`, `agents.launchInTab` (+ toute string introduite dans les Tasks 4/6/7).

- [ ] **Step 1 : Recenser les clés réellement utilisées**

Run: `grep -rno "t(\"terminal\.[a-zA-Z]*\"\|t(\"settings\.terminal[a-zA-Z]*\"" apps/desktop/src/components apps/desktop/src/composables`
Expected: liste des clés à fournir.

- [ ] **Step 2 : Ajouter les clés dans `en.ts` (référence)**

Ajouter sous les sections `terminal` / `settings` / `agents` (créer la section `terminal` si absente) :

```ts
  terminal: {
    newTab: "New terminal tab",
    hide: "Hide terminal",
    rename: "Rename tab",
    close: "Close tab",
  },
```
et dans `settings` :
```ts
    terminalFontSize: "Terminal font size",
    terminalShell: "Terminal shell",
    terminalShellHint: "Leave empty to use your default shell",
```

- [ ] **Step 3 : Traduire dans `fr.ts`, `es.ts`, `pt-br.ts`, `zh-cn.ts`**

Mêmes clés, valeurs traduites. (Utiliser le skill `i18n-sync` pour garantir la complétude des 5 locales.)

- [ ] **Step 4 : Vérifier la complétude**

Run: `cd apps/desktop && pnpm vitest run` (les tests i18n existants vérifient la parité des clés si présents) ; sinon comparer manuellement.
Expected: aucune clé manquante.

- [ ] **Step 5 : Commit**

```bash
git add apps/desktop/src/locales/
git commit -m "i18n(terminal): add terminal + settings keys in 5 locales"
```

---

## Task 9 : Vérification finale + documentation

**Files:**
- Modify: `ROADMAP.md`, `CHANGELOG.md`, `website/changelog.md` (à la livraison/tag uniquement — pas pendant le dev)

- [ ] **Step 1 : Suite de tests complète**

Run: `cd apps/desktop && pnpm vue-tsc --noEmit && pnpm vitest run`
Expected: PASS (tous les tests verts).

- [ ] **Step 2 : Build Rust complet**

Run: `cd apps/desktop/src-tauri && cargo build`
Expected: PASS.

- [ ] **Step 3 : Test manuel desktop complet (Tauri)**

Run: `cd apps/desktop && pnpm dev`
Vérifier (vrai PTY) :
- Terminal s'ouvre, `vim`/`htop`/`claude` fonctionnent (TUI interactif).
- Multi-onglets `+`, `×`, switch `⌘1..9`, `⌘T`/`⌘W` quand focus terminal.
- Renommage double-clic ; redimensionnement hauteur ; titre auto via OSC.
- "Launch Claude Code" depuis Agent Sessions → onglet PTY avec `claude` lancé ; pas de faux "active".
- Cursor/Windsurf → ouverture GUI externe.
- Changer de repo : les onglets du repo précédent restent vivants en arrière-plan.
- Quitter l'app : pas de PTY orphelin (`ps aux | grep -c <shell>` stable).

- [ ] **Step 4 : Vérifier l'absence de régression de référence GitTerminal**

Run: `grep -rn "GitTerminal" apps/desktop/src`
Expected: 0 résultat.

- [ ] **Step 5 : Vérifier le repo git (pas de branche/commit parasite)**

Run: `git status && git log --oneline -12`
Expected: uniquement les commits de ce plan, sur la branche de feature.

- [ ] **Step 6 : Documentation (au moment du tag uniquement)**

À faire lors de la release v3.1.0 (via le skill `release`) :
- `ROADMAP.md` : déplacer "fix Launch Claude Code" + "Terminal with tabs" vers **Shipped** (v3.1.0) ; garder "AI workspace" en Planned.
- `CHANGELOG.md` (format Keep a Changelog) + `website/changelog.md` (narratif), même commit.
- `apps/desktop/CLAUDE.md` : noter l'invariant "instances xterm hors réactivité Vue".

---

## Notes de risque

- **`portable-pty` Windows** : `CommandBuilder` + `-l` n'a de sens que sur Unix (le `#[cfg(not(windows))]` le gère). Tester `powershell.exe`/`cmd.exe` séparément.
- **dev:web** : le mock shell `-i` en mode pipe n'est pas un vrai TTY — `claude`/`vim` n'y fonctionneront pas (attendu). Le mock sert à exercer l'UI (onglets, output, resize, renommage), pas les TUI.
- **`onTitleChange`** : tous les shells n'émettent pas d'OSC titre par défaut. Fallback = nom du shell ; ce n'est pas un bug si le titre ne change pas tout seul.
- **Channel import** : `import { Channel } from "@tauri-apps/api/core"` — vérifier que la version de `@tauri-apps/api` du repo l'expose (Tauri 2 oui).
